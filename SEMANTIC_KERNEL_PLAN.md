# Semantic Kernel Refactoring Plan

## Current entry

**Profile-native proposal/persistence/recovery is complete in this checkpoint. Next bounded scope: read-only attachment design for consequential assessment and delegated Work.** Parent recovered the timed-out implementation, reviewed/fixed the actual source and added adversarial/native tests: **187/187 focused; 489/489 full**, no failures or skips, no blocking diagnostics. The approved A decision remains settled: common Kernel-defined Trace meanings, exact profile/build/source grounds and separate Actor authorization. No writer is active; no new executable lane is assigned. First trace how the existing Gate/Work/reducer mechanisms can consume the exact profile Change reference, then record a concrete bounded implementation scope. Do not broaden Event 2/Trace 15 or imply consequential acceptance merely to continue the pipeline.

The completed slice started in `/home/canina7/Projects/codewiki`, branch `audit/clean-baseline`, at `3778a6651d03d1fe0e66f6222677655c0ea8fd97` with an empty index. Verify the current exact HEAD/worktree before the next scope. This checkpoint owns 22 executable paths, this ledger and the one approved `semantic-kernel.md` clarification; unrelated Wiki alignment, settings, four outputs and lifecycle history remain preserved. No activation, controller handoff or dogfood qualification follows.

Subagents implement bounded Work; the parent scopes it, reviews actual code/tests, fixes findings, validates final bytes and decides readiness. No reviewer-subagent gate. Routine decomposition/fixes need no new approval; material intent, design, scope or authority changes require the maintainer. Subagents use OpenAI Luna; parent remains Astra. Delegation failure requires captured partial state and authorized parent takeover, not blind retries or unapproved provider/executor changes.

## Delivery sequence

| Milestone | Status |
| --- | --- |
| Passive exact material foundation | Complete through `791109494268e38b7d6952f1d1ef9b308ade4456`. |
| Internal Wiki interpretation | P1-R complete at `6510b791cedb432a1cbf8cc1973945feeb1a95bc`: explicit profile, bounded inert YAML/Markdown, lexical lineage, exact type contexts; legacy readers unchanged. |
| Exact profile-source composition | Complete at `707c266172f15240a7330ec9cf389c28a146de07`: canonical/Change/commit resolution once, managed-only interpretation, byte preflight, historical/reopen binding and read-only native coverage. |
| Explicit profile transactions | Complete at `3778a6651d03d1fe0e66f6222677655c0ea8fd97`: exact mappings, lineage constraints, potential type impacts and deterministic identity. |
| Profile proposal and recovery | Complete in this checkpoint: explicit selection, Change/Event 2, Trace 15, existing append/reducer/CAS, retained exact sources and fresh-server recovery. |
| Change-first backend | **Unfinished.** Attach consequential Gate/decision assessment and delegated Work to profile Changes, then commitment/completion and honest adoption/history. No parallel lifecycle or fabricated acceptance. |
| External first dogfood | Unqualified. Freeze immutable bytes and exercise the complete path in a disposable external Project with isolated settings before any separately authorized activation/handoff. |

Prioritize the smallest complete truthful backend path. [Wiki](.codewiki/wiki/items/system/components/wiki.md) owns desired representation; [Change](.codewiki/wiki/items/system/components/change-trace.md) and [Planning](.codewiki/wiki/items/system/components/planning.md) own Change-level commitment and delegated execution. Parsing, type resolution and transaction admission do not establish semantic adequacy, acceptance, truth or permission. Assessment must preserve consequential and honestly unresolved outcomes, not substitute headings, keywords or empty policy.

## Transaction closeout

Reviewed checkpoint scope: `src/kernel/wiki/profile-transaction.ts`, the narrow admission-helper extraction in `src/kernel/wiki/profile.ts`, `tests/kernel/wiki/profile-transaction.test.mjs`, native transaction assertions in `tests/server/queries/profile-source.test.mjs`, exact inventory/private-closure assertions in `tests/kernel/invariants/architecture.test.mjs`, and this parent-owned ledger. No dependencies, public/legacy contracts, lifecycle writers or Store/port changes.

`validateProfiledWikiTransaction` explicitly binds `WIKI_PROFILE_ID`, a Kernel Build digest, a canonical responsible Change path, two complete same-repository/object-format snapshots, and recomputed type contexts from re-admitted files. Explicit path/blob mappings cover every changed endpoint exactly once: add (0→1), revise (same-path 1→1), rename (different-path 1→1), split (1→many), merge (many→1), retire (1→0). No inferred continuity, fabricated Item IDs or reservation store. Ordinary revision/rename preserves resolved origin target sets; explicitly resulting files' revision references resolve to the responsible Change. Split/merge retains exact old/new subjects without invented origin-union or attribution policy. Unchanged Items can have potential type impacts without changing their revision metadata.

The identity digest binds profile/build/Change path, snapshots, mapping grouping, both contexts and impact subjects; exact non-NFC path bytes are hashed rather than normalized. Returned source text/reference spelling is preserved. **The digest is an identity-record commitment, not a signature authenticating arbitrary serialized parser output.** Store/adapter/caller must establish exact bytes and membership. JSON re-admission alone proves only internally coherent supplied claims; native restart tests separately reload both exact historical commits and compare complete admitted results and serialized bytes. Any later Gate material encoding must preserve this distinction and cannot blindly feed raw non-NFC source text to the legacy NFC-only canonicalizer.

Parent fixes and tests:

- Bound **aggregate** mapping endpoints to 512 per side before overflow endpoint inspection, not merely 1024 mappings × 512 endpoints per mapping. Inclusive tests admit 512-file sides and 1024 explicit mappings.
- Reject contradictory facts for one claimed commit and contradictory managed inventories for one claimed tree; retain same-blob/different-text rejection. These are consistency checks, not Git authentication.
- Reject control/BOM characters in responsible Change paths. Remove speculative compatibility aliases; preserve concrete helper return inference and document admission provenance.
- Use a unique private temporary directory for serialized native evidence. Compare complete re-admitted records and exact serialization after reopening SHA-1/SHA-256 sources.
- Add lineage/move, incomplete snapshot, mixed format, hostile access, exact Unicode digest, mapping grouping/order, nested freezing and post-call mutation tests. Corrected parent test-oracle mistakes (mapping cardinality precedes membership; decoded records intentionally restore null-prototype metadata); failed logs retained, production policy not weakened.

Final primary LSP has no errors; scoped multi-runner scan has no blocking errors. Remaining complexity, nested-conditional, small helper duplication, private-export and admission-boundary warnings are recorded as non-blocking; existing architecture deprecation remains. This is not a globally warning-free or vulnerability-qualified verdict. The writer's 458-test gate is earlier evidence, superseded by the parent final-byte gate.

Both transaction sides currently require complete valid profile contexts. Empty bootstrap, raw/legacy adoption, patch writing, link adequacy, semantic acceptance, further lifecycle/public schema evolution and authority conversion remain unfinished. No cache, candidate registry, history store or new persistence port was added.

## Completed slice: profile-native proposal and recovery

Parent tracing established the actual coupling:

- `ChangeTarget`, proposal targets and Work targets use namespaced Item IDs/facets. `validateWikiTransaction` pairs by those IDs and uses legacy semantic digests and retirement reservations.
- `prepareWikiPatch` writes proposed blobs, decodes legacy files and derives component event owners; its in-memory candidate retains the old snapshot. A profile transaction instead requires two real exact snapshots—never relabel that draft as a committed tree.
- `validateProposalScope` checks target coverage and `actor.wikiItemIds`. `loadCanonical` / `loadLifecycleChange` still require legacy materialization. Wrapping profile content in old envelopes or inventing IDs would bypass real identity/authorization constraints.
- `evaluateGate` already canonicalizes selected materials, binds hashes into Gate identity and persists immutable bundles. `commitDecision` already requires persisted passed Gate evidence and canonical/Change CAS. Reuse these mechanisms rather than build a parallel lifecycle.

The compatibility approach, owned paths and negative/restart requirements below were set before mutation. Later assessment/Work attachment must likewise bind exact selected profile/build/type grounds and transaction subjects, preserve old protocols and restrictive grants, and avoid Item-ID shims or silent authority widening. Bootstrap and honest adoption need explicit admission, not parser fallback or fabricated history.

### Approved semantics and compatibility

The maintainer chose A after clarifying that Trace events are lifecycle facts, not project-specific Change categories. Kernel-defined event meanings are shared across Projects; broad classifications and actual intent/context/commitments condition Work and scrutiny. Do not introduce a `profile` Change category, project-specific event catalog, new category taxonomy or per-event Wiki ownership metadata. The parent recorded this desired behavior in `semantic-kernel.md`; Actor and protected-effect authority are unchanged.

Preserve Change `1.0.0`, Work `1.0.0`, Event `1.1.0` and Trace `14.0.0` constructors, grammar, canonical bytes and digests. New profile representation uses `codewiki.change@2.0.0` and `codewiki.change-event@2.0.0`; only `change.proposed` is admitted in that event version in this slice. Use `codewiki.change-trace@15.0.0` for the explicitly selected new Trace format, rather than broadening the old Trace grammar invisibly. Trace 14 admits legacy events only; Trace 15 admits the new event format only. Cross-mode/mixed append and migration are rejected. Share the existing Trace framing, bounded decoder, append writer and Change reducer; version-specific payload admission is not a second lifecycle. Old exported constructors/types may remain narrowly legacy with explicit new entrypoints/overloads to avoid unsafe casts or widening old caller assumptions.

The new Change preserves existing intent, rationale, acceptance, relationships, classification and realization fields. Its targets are nonempty technical endpoints `{kind: "profile", transactionDigest, side: "before" | "after", pathUtf8Hex, blob}` derived from the admitted mappings, not caller-fabricated legacy Item IDs/facets. This endpoint-requiring operation is not yet the general no-Wiki-delta proposal path. A new immutable `codewiki.profiled-wiki-reference@1.0.0` carries `profile`, `kernelBuildDigest`, both complete `ProjectSnapshot` records, `changePathUtf8Hex`, encoded explicit mappings and `transactionDigest`. No parser-output text/metadata is serialized as authenticated content. The event replaces `ownerItemId` with `ownerBinding: {kind: "kernel", profile, kernelBuildDigest, transactionDigest}`; event kind/protocol and Change attachment supply the rest of its exact grounds. All duplicate identities must agree. Build provenance for a new proposal is service-configured, not whatever build a caller claims. Historical recorded build grounds are not rewritten to the current service build on reopen.

`pathUtf8Hex` means canonical lowercase, even-length hexadecimal encoding of the exact UTF-8 path bytes, without normalization or a `0x` prefix. Reject invalid UTF-8, noncanonical tokens, invalid paths, overlong paths and aggregate transport/node budgets before unbounded expansion; preserve existing profile/transaction and canonical-envelope limits. A valid raw transaction can exceed the stricter transport budget and must then fail explicitly. Rebuild references from adapter-backed exact sources on reopen and compare complete canonical references as well as transaction digests. A digest is not a signature or proof that a recorded build ran.

### Executable slice

1. Add optional, explicitly admitted `wikiProfile: WIKI_PROFILE_ID` to internal Project Server/read configuration. Absence retains existing legacy behavior, with no Markdown autodetection. Unsupported selection or profile mode combined with nonempty legacy retirement reservations is rejected rather than silently converted. This is a representation choice, not project-specific Change semantics. Do not change persistent local Project configuration/bootstrap in this slice.
2. Add one command, `changes.propose-profile`, to the existing transport/lifecycle dispatch. Input is singular: `commandId`, explicit bounded `changeId`, `expectedProjectHead`, `profile`, `kernelBuildDigest`, `afterCommit`, `proposal` and encoded `mappings`. `proposal` carries the existing Change descriptive fields and resolved `{kind, changeId}` relationships; it has no target list, Wiki patch or batch-local proposal references. The before selector is the exact expected Project head. Derive the responsible path with existing `tracePath(changeId)`; do not invent a TRACE filename convention, reservation store or title/path-based Item ID. Explicit Change identity permits preparing the immutable candidate's revision references before submission; CAS and command-digest replay still prevent collision/overwrite.
3. Authorize operation and scopes before replay-cache reuse or any Store effect. Profile operations require `actor.wikiItemIds === null`; profile creation also preserves the existing `actor.changeIds === null` restriction. Reads enforce any Change-ID restriction before source reads. Existing grants do not automatically grant the new operation. Keep checks at both the server boundary and directly callable command/query boundary. Profile mode supports only this proposal command and existing `changes.read` with `view: "get"` in this slice; other operations return explicit unavailable before writes/check execution. Do not manufacture a Gate pass, invoke models, or claim operational capabilities that are not implemented.
4. Resolve canonical head once and require it to equal the expected before commit. Load both exact profile sources via `loadProfiledWikiSource` and validate their explicit transaction. Reject candidate changes outside managed Wiki Markdown, candidate modification of existing Trace/history, and an already present responsible Trace path, before preparing writes. The complete immutable candidate is not `prepareWikiPatch`'s old-snapshot draft. Reuse bounded tree reads to check the complete delta, not just Markdown inventories.
5. Write only the new Trace onto the candidate's existing exact tree through `appendTraceCommit`; do not rewrite its non-NFC Wiki paths through the NFC-only mutation API. Retain both before and candidate commits through real parent reachability (deduplicated parents), including when the candidate is on a separate same-repository branch. The enclosing Trace commit is a new subject, not the pre-Trace candidate identity and not an assertion of acceptance. Atomically CAS the new managed Change ref while verifying the unchanged canonical head in the same existing Store CAS transaction. The native Store permits an expected-old/new-equal canonical guard; its ref list must remain sorted and unique. No canonical OID movement occurs.
6. Reopen through existing `changes.read`/`view: "get"`, explicitly profile-configured, using a bounded profile-aware source/Trace helper and the same reducer. Support managed Change-ref and exact-commit sources; require Trace/Change/Project identity binding, exact before/after source re-admission and complete reference equality. Return canonical-safe versioned Change/state and technical bindings, not raw non-NFC parser results or legacy component ownership. Test a fresh server and adapter instance, not an in-memory replay cache. Same-command retries must recover the recorded outcome without duplicate events/objects/refs; differing content under the same identity must conflict.

No Work protocol/integration, Gate verdict, Decision commitment, completion, adoption, bootstrap, live conversion or external package qualification is part of this slice. Keep unsupported profile transitions visibly unavailable. Unrestricted later capability development remains within the backend objective; operating protected production effects still needs separate authority.

### Authorized implementation/recovery scope (closed)

The timed-out fresh-context Luna writer was restricted to the following paths; unused entries remained unchanged. Parent took over only after capturing the stopped run's partial state. These are checkpoint provenance, not a grant for the next executable lane.

Source: `src/api/contracts/command.ts`; `src/kernel/changes/contracts.ts`; `src/kernel/changes/events.ts`; `src/kernel/changes/trace.ts`; `src/kernel/changes/reducer.ts`; new `src/kernel/wiki/profile-reference.ts`; `src/server/authorization/policy.ts` (narrow profile-scope helper only); `src/server/commands/lifecycle.ts`; `src/server/commands/repository.ts`; new `src/server/queries/profile-change.ts`; `src/server/queries/source.ts` (explicit configuration/legacy guard only); `src/server/index.ts`.

Tests: new `tests/api/contracts/profile-lifecycle.test.mjs`; `tests/api/transport/envelope.test.mjs`; `tests/kernel/changes/contracts.test.mjs`; `tests/kernel/changes/events.test.mjs`; `tests/kernel/changes/trace.test.mjs`; `tests/kernel/changes/reducer.test.mjs`; new `tests/kernel/wiki/profile-reference.test.mjs`; new `tests/server/commands/profile-lifecycle.test.mjs`; `tests/server/authorization/policy.test.mjs`; `tests/server/index.test.mjs`; `tests/kernel/invariants/architecture.test.mjs`.

Architecture tests must replace the now-obsolete exclusion of composed profile modules from the Product entrypoint closure with exact authorized importer/reachability assertions. Preserve adapter-only YAML/Markdown parsing, effect-free Kernel boundaries and package exports; no new public package export, dependency, port or ambient Kernel effect is authorized. Do not delete the fences wholesale. All other paths, including existing profile parser/source/transaction modules, Work/Gate modules, API read grammar, adapters, ports, manifests, settings, outputs and Wiki/history, remain protected. Ask the parent before expanding paths or changing the wire/protocol rules; do not fall back to a parallel lifecycle or legacy-ID shim. Parent owns the plan and desired-state edits.

### Acceptance and evidence

Required focused tests: exact old constructor/decoder bytes and digests; old grammar rejects new payloads; Trace 14/15 cross-mode and mixed append rejection; same reducer yields proposed state without acceptance; bounded hostile/canonical UTF-8 token admission; complete reference/owner/build/snapshot/membership checks; native SHA-1/SHA-256 proposal → Trace CAS → fresh-adapter/server read and same-command replay; non-NFC path preservation; retained before/candidate reachability; candidate/history/protected-path rejection; no-write scoped/operation denial, including before cached replay; unavailable Gate/Work/commit paths; stale head and concurrent CAS races. Check ref/config/index purity around reads. Pre-write rejection means no object writes; a race after authorized preparation means no ref movement, but unreachable prepared Git objects may remain. Do not promise object rollback.

Use primary LSP before builds, focused legacy/new/native tests and final full `npm test` on completed executable bytes; then `lens_diagnostics` mode `all`. Record failed and successful commands/exits/counts, actual tested hashes and scanner limitations. Preserve byte/mode evidence, not filename-only status. Writer restrictions were: no staging, commits, progress.md, reviewer child, hidden subprocess agents or fallback executors; report near 35 minutes rather than silently timing out at 45 minutes. Parent alone reviews/fixes actual code, validates final bytes and makes an authorized local checkpoint.

### Parent closeout and evidence

Evidence: `~/.local/state/codewiki/evidence/profile-lifecycle-4wr8ddfo/timeout-recovery/`. Run `4353f90c-fa2d-434d-8315-3672965ac302` failed with `Subagent timed out after 2700000ms.`, leaving 12 source changes and no tests. Incoming copies/diff/tool evidence and failed child/parent logs remain preserved. Parent verified 195 other baseline paths/modes and the empty index before takeover; there was no retry or provider/executor switch. The earlier read-only brief remains under `~/.local/state/codewiki/evidence/lifecycle-attachment-bkxtm7s7/`.

Parent repairs and accepted exceptions:

- Attach the exact reference to Change, carry the admitted **transaction** digest rather than hashing a smaller reference body, derive complete targets, and use common Trace framing, append writing and proposal reduction. Independent legacy golden hashes were captured from immutable `3778a665` source; old Change, complete Trace (including Work/events) and reduction bytes still match.
- Separate operation authorization from the internal exact loader so proposal-only capability can recover its own command without acquiring general reads. Scope checks precede replay; cache keys include authority identity. Historical build grounds remain recorded, not relabelled. Canonical observation failures are not disguised as stale heads.
- Re-admit exact sources and compare complete references/digests on reopen, including containing-snapshot placement and parent retention. Test native SHA-1/SHA-256, non-NFC names, pruning after candidate-ref removal, historical builds, forged consistent references, lost CAS acknowledgements, stale/raced heads and no-effect denials/unavailable transitions.
- Use configured document/byte ceilings plus separate corpus bounds: 65,536 entries and **4 MiB aggregate path bytes**, not YAML collection count or a per-Item path allowance. Profile per-Item/context bounds still apply. Hostile access, complete identities, immutable results, invalid UTF-8 and transport text/node overflow are tested.
- Add shared `tests/kernel/wiki/profile-fixtures.mjs`. Parent also bounded repair to `src/adapters/git/project-store.ts` and its existing test: preserve inherited native names without granting nonportable mutations; share listing/tree construction; hash full leaf inventories read-only and require exact native tree reproduction. Reject hidden empty trees/normalized malformed modes before proposal writes; preserve prefix-read behavior and use iterative tree construction. **45 other adapter functions/methods are byte-identical**, including mutation/authority/ref/root-binding policy. Store preparation failures may still leave unreachable objects; no rollback promise.
- Remove the source-graph cycle through a four-field, already-admitted event projection in `src/kernel/changes/snapshot.ts`; **all 16 existing function bodies are unchanged**. Replace only obsolete private-reachability fences with exact authorized import edges; acyclic graph, pure Kernel, adapter parsing and package-export fences remain enforced.

Final executable evidence: `before-final-gates-2.json` records 22 tested hashes; `focused-parent-final.log` is **187/187**, exit 0; `npm-test-parent-final-2.log` is typecheck/build/test-smoke **489/489**, exit 0, no failures/skips. Earlier failures include real protocol/native-tree defects and corrected fixture/oracle mistakes (Work identity, limit consistency and intentionally malformed Git objects); no production invariant was weakened to obtain passing tests. Final primary LSP and session error diagnostics are clean. The multi-runner scan retains non-blocking complexity, duplication, private-export and boundary-style findings; Go tooling is inapplicable, trivy was not opted in, and no applicable dead-code/test-runner cache verdict exists. This is not a global warning-free or vulnerability qualification.

Preservation is byte/mode based: 182 protected-scope paths are unchanged; 188 of the 205 writer-baseline paths remain identical, including unused authorized paths and the prepared Wiki clarification. `protected-members.json`, `review.md`, `validation.json` and the external checkpoint receipt record final proof. Only the 22 reviewed executable paths, this ledger and the approved semantic-kernel paragraph belong in this checkpoint. The profile slice does not implement Work/Gate acceptance or adoption. No production canonical OID movement, activation or controller handoff was performed.

## Preservation and validation authority

One writer per checkout. Use native coding tools and ordinary tests, never CodeWiki `wiki_*` tools or `/wiki-*` commands, mutable controllers, project-local CodeWiki installs/links/skills/pins or dogfood state. Preserve unrelated work, Wiki envelopes/IDs/provenance, `.codewiki/changes/**`, repository bindings and all paths outside the active Work. No reset/stash/transplant or competing writer.

Ordinary reviewed local checkpoints are authorized; stage only owned reviewed paths. Push/fetch, canonical-ref movement, live conversion, credentials, paid Product experiments, activation and controller handoff need applicable explicit authority. Disposable native Git fixtures may create fixture objects/refs. Candidate packing/testing belongs in disposable external Projects with isolated Pi settings. Runtime/private/generated/package artifacts are not source truth and do not belong in project Git.

Use primary LSP before builds, scoped adversarial/regression gates and a final full `npm test` on completed executable bytes. Record commands, actual exits/counts, tested hashes, scanner limitations and all protected-state comparisons outside Git; verify committed bytes and empty index. Finite parser/admission budgets are not an OS sandbox or a universal pre-allocation guarantee.

## Evidence and cold history

- Transaction checkpoint: `~/.local/state/codewiki/evidence/profile-transaction-28f62ng2/`. Original 203-path hashes/modes, plan and patch baseline; run `70ff202a-8d56-4e98-95fe-61ae442291d6`; parent review/final logs, preservation and checkpoint under `parent-review/`. Incoming plan differed by one formatting-only blank line, accepted by parent; all other protected bytes/modes matched. The writer's filename-only preservation report was not byte proof.
- Exact source composition: `~/.local/state/codewiki/evidence/profile-source-k66u0rke/parent-review/` (183 is the new combined transaction gate; source checkpoint had 166 focused/448 full tests).
- P1-R final review: `~/.local/state/codewiki/evidence/p1-reader-6q45akhe/parent-review-fm2ircgg/` (101 focused/435 full). Its parent directory retains credit failures, owner-stopped Flash run, Luna timeout and unnecessary reviewer launch failure; none is an active gate.
- Passive material repair: `~/.local/state/codewiki/evidence/change-first-execution-mbmnl26f/parent-takeover/`.
- Compatibility audit: `~/.local/state/codewiki/evidence/wiki-profile-design-v5vlimyr/`; corrected inventory: 47 Markdown Items.
- Earlier plan: `git show c04c00786c242e7fa32522d6b1eb902751af6d99:SEMANTIC_KERNEL_PLAN.md`. Use only for a specific historical question; cold pauses are not current instructions.
