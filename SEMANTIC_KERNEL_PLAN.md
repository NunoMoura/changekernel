# Semantic Kernel Refactoring Plan

## Current entry

**W1-DOC is complete: the agreed Wiki model is aligned in desired-state documentation.** The maintainer agreed to the six core Item-type categories and then explicitly accepted fixed `.codewiki/wiki/` and `.codewiki/wiki/types/` structural homes, with optional Project-specific domain folders, and asked to proceed. The bounded packet below changed only this plan and the bodies of the Wiki, Project and Change component specifications. Their existing envelopes/IDs/provenance, every current file location, lifecycle records and executable work are preserved. **W1-PROFILE's P1 recommendation is ready for owner review**, following the maintainer's instruction to proceed under parent PM management with dev delegation. The read-only dev audit completed; parent source verification corrected several overbroad findings. The P1 decision table, compatibility matrix, desk examples and next source-scope recommendation below are design outputs, not accepted schema or implementation. The parent owns this plan and stable Wiki alignment; developer reports supply evidence, not acceptance. Only this plan changed in W1-PROFILE; existing Wiki envelopes and all executable work remain preserved.

**SC-2B remains locally implemented but unaccepted; SC-2B-R stays paused.** The historical unchanged suite passed **373/373**, including typecheck/build, but external probes reproduced late limit admission, empty-file boundaries, lost Store failure categories and native Git filename/order defects. This documentation work does not repair or requalify that subject. Revalidate its bounded source packet against the agreed distinction between Project-wide source material and managed Wiki knowledge before requesting source approval. No push, migration, public API expansion, package activation or controller handoff is authorized.

SC-2A and SC-2R retain their completed bounded validation in checkpoint `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`. Their earlier evidence does not qualify SC-2B or complete SC-2. SC-1 findings remain conditional desk reasoning, not results from an implemented semantic evaluator. This file remains the sole active roadmap and procedure/status ledger; Wiki owns stable desired behavior. Older packets do not authorize additional work.

## Baseline and protected work

- Checkout: `/home/canina7/Projects/codewiki`; branch: `audit/clean-baseline`; SC-1 capture HEAD: `0165f1b6cfe20751354114d4a6e9c20445092fb4`. It is a documentation checkpoint over source ancestry `53b17a023beeb8edde48b7cf488dcdfe95639e7a`; selected audit ancestry remains `7fe04257d2a18d5d16130c45f5919e49bb84f92d`. Verify current Git state; none of these is an instruction to reset.
- Sole canonical remote: `origin`, `git@github.com:NunoMoura/codewiki.git`. No remote baseline or controller authority is inferred from a cached ref, timestamp or historical receipt.
- The pre-repair D4 patch remains captured as unaccepted history. SC-2R now authorizes bounded repairs to `src/adapters/git/project-store.ts` and `tests/adapters/git/project-store.test.mjs`; preserve `src/adapters/git/local-server.ts` byte-for-byte. Its infrastructure/provider failures do not authorize another executor, provider, paid call or lane retry. Reassess the patch under SC-2, not the old packet.
- Preserve the frozen `tests/adapters/git/local-read-purity.test.mjs`, user `.pi/settings.json`, existing `outputs/`, `AGENTS.md`, governed `.codewiki/changes/**`, existing archived documents and all unrelated work. The Project Wiki repository-binding safeguard and current Wiki envelopes/IDs/provenance/ownership remain unchanged in this pass.
- SC-2A preservation capture: `~/.local/state/codewiki/evidence/sc2a-corpus-68j90228/`, at the same exact HEAD and branch above. The prior draft and all 193 other captured paths were verified unchanged before starting. Only this plan, the two new corpus files and the scoped architecture assertions below may change. The index was empty and refs were unchanged.
- SC-1 private preservation artifacts: `~/.local/state/codewiki/evidence/sc1-contract-probes-kw87hbb0/`. `state-before.json` covers 194 tracked/output paths plus branch, HEAD and refs; the sole allowed edit is this plan. The maintainer's intervening extension update did not change those captured bytes/modes. Prior documentation evidence remains under `semantic-hot-cold-u5ep4_nb/`. These are checks/backups, not another source checkout, semantic validation or release qualification.

## Native working procedure

1. Work in the single active source checkout on the selected proposal branch. Reconcile unique work before baseline changes or cleanup; do not create competing development checkouts or transplant donor trees wholesale.
2. Follow the current SC queue. Before a source slice, record resolved design choices, exact file/test scope, preserved-work disposition and bounded acceptance here. Unscheduled gaps enter this plan first. R1's native working method and data-contracts naming amendment remain; its superseded destination, queues and internal Check rubrics do not.
3. Ordinary native edits, diagnostics, tests and reviewed Git checkpoints within approved scope require no separate Change admission, release, prospective-OID permission or controller swap. Record actual command outcomes and exact subjects; missing evidence is not success. Read known symbols directly; use compact outlines for orientation and targeted LSP queries for definitions/references/types. Inspect one unfamiliar query result before batching it. Use proactive diagnostics, inspect exit status, and stage only reviewed owned changes. Structural checks and keyword assertions do not establish prose meaning or semantic coverage.
4. Do not load CodeWiki from mutable code or this checkout's `.pi/`, or invoke CodeWiki `wiki_*` tools or `/wiki-*` commands here. Do not add package links, duplicate skills, executable Plugin paths or controller pins. Candidate N+1 never governs itself.
5. Push, canonical-ref movement, live conversion, credentials, paid model calls, activation and controller handoff need applicable explicit authority. Pack/test candidates only in disposable external projects with isolated Pi settings. Worktrees are not sandboxes; unknown external effects require reconciliation before retry.
6. Preserve supported source-checkout storage and provenance. `.codewiki/wiki/**` is desired-state truth, `.codewiki/changes/**` lifecycle truth, and `src/**`/`tests/**` executable truth. Keep `.codewiki/kb/**` and `.codewiki/traces/**` absent; never dual-read/write. Project-wide source admission does not authorize stripping envelopes, re-bootstrap, live migration or reclassifying every Markdown file here as governing policy.
7. Qualification requires an exact committed, uniquely versioned immutable candidate, supported profile, frozen budgets and replacement guidance. Post-freeze corrections require a new subject/version and qualification. Resolve actual independent controller authority and state compatibility; activation alone is not handoff. Required handoff needs quiescence and no overlap. Higher-priority truth/safety/source-checkout restrictions do not sunset with bootstrap procedure.

## Design entry points

- [Change](.codewiki/wiki/items/system/components/change-trace.md) is the governed origin/evolution point for accepted knowledge; [Wiki](.codewiki/wiki/items/system/components/wiki.md) has a predictable managed home and expresses current accepted meaning, not every source document or disposable model output.
- [Project/Git](.codewiki/wiki/items/system/components/project.md) supplies native intent-bearing records, exact Wiki outcomes and history. It is foundational, not an optional connector. Retired knowledge stays available through native history; there is no separate Archive or Sources knowledge system.
- [Evidence/information](.codewiki/wiki/items/system/components/evidence.md) defines on-demand material and its scoped evidential use. [External connectors](.codewiki/wiki/items/system/components/provider-boundary.md) optionally add users' data; the core must work with none configured. Retrieval/context eviction does not promote or retire knowledge.
- [Semantic validation](.codewiki/wiki/items/system/components/checks.md) and the [four loops](.codewiki/wiki/items/system/flows/change-lifecycle.md) govern transitions. Decluttering must preserve current grounds and obligations, not merely conclusions.

Model context is transient; Wiki preserves current accepted knowledge, and Git retains exact historical outcomes.

## Implementation disposition and gaps

The existing source/test map is retained evidence, not a fresh source qualification. SC-1 used targeted reads of `wikiFileFormat`, `decodeMarkdownItem`, `readExactWiki`, `readWikiTree`, `executeWikiRead`, Project Store request types, Change contracts and architecture guards. Those reads confirm root/envelope/Item coupling, not runtime correctness. Regression assets remain evidence to reuse, not newly passing acceptance results.

| Area / existing evidence | Disposition and remaining gap | Slice |
| --- | --- | --- |
| `src/kernel/data-contracts/`, `src/kernel/identity/`, matching tests | **Retain** bounded typed contracts and exact/digest identities. **Adapt** Build binding: a policy digest does not identify all executable bytes. | SC-2/4/7 |
| `src/kernel/wiki/`, `src/adapters/git/wiki.ts`, `src/server/queries/wiki.ts`, Wiki tests | **Adapt** exact reads, atomic post-state validation, citations, ownership and isolated legacy provenance. **Separate** Project-wide source admission from the managed Wiki home and its eventual declared profile; do not mechanically remove all root/structure requirements. Resolve profile compatibility before replacing envelope contracts. Add current-role/adoption boundaries, passage retirement, native historical retrieval with disposition, and bounded cold-context access. Existing field/body diff is not consequential semantic interpretation. | SC-2/3/4/6 |
| `src/kernel/changes/`, Change/lifecycle tests | **Adapt** intent/rationale, exact snapshots and append-only outcomes. Replace fixed type/realization and passed-Gate assumptions where necessary. Bind promotion, revision, retirement and re-adoption to accepted scoped transitions; retain historical grounds without making retrieval a transition. | SC-1/4 |
| `src/kernel/gates/`, `src/ports/check-runner.ts`, `src/server/commands/gates.ts`, Gate tests | **Replace** authored Pack selection as policy; retain useful exact-subject, evidence, stale-result and failure mechanics. Add supported/contradicted/unresolved judgments, applicability and coverage. Empty Gates and relabeled Packs are not the new evaluator. | SC-3/4 |
| `src/kernel/work/`, `src/server/commands/lifecycle.ts`, `src/server/queries/project.ts`, Work/lifecycle tests | **Adapt** dependency, assignment and transition mechanisms for shared obligations, uncertain paths, partial outcomes and actual join assessment. Planning applies to knowledge work too. | SC-4/5 |
| `src/adapters/git/project-store.ts`, `src/ports/project-store.ts`, `src/server/commands/repository.ts`, Git tests | **Retain/adapt** native snapshots/history and expected-old-OID writes; no optional Git connector or duplicate archive store. Define retention, subject binding and rewrite/transport behavior before changing it. Reassess D4 separately for roots, redirects, linked worktrees, object formats, bounds and read purity. | SC-2/4/5/6 |
| `src/server/index.ts`, `src/server/authorization/policy.ts`, `src/server/queries/source.ts`, `src/api/`, `bin/codewiki.mjs`, auth/transport tests | **Retain/adapt** authorization, redaction, once-resolved sources and honest capabilities. Distinguish current knowledge, pending work and historical/external information in responses. Resolve Server/Git layering deliberately. External data connectors are optional, not a replacement for native storage or acceptance. | SC-2/3/4/6 |
| `src/server/recovery/facts.ts`, `src/kernel/evidence/reference.ts`, recovery/evidence tests | **Adapt** immutable subject/evidence binding; process-local facts do not supply durable judgments or restart custody. RAG indexes remain rebuildable, not source truth. Preserve exact source versions, adoption/retirement provenance, availability/permission limits and independently supported claims. | SC-4/6 |
| `src/ports/agent-runtime.ts`, `src/adapters/dsh/`, runtime tests | **Adapt** a bounded observation/execution boundary; defer selecting DSH as the executor. Injected/replay tests do not establish real tools, containment or quiescence. One enforceable execution profile is enough. | SC-1/3/5/6 |
| `src/ports/preview.ts`, `src/adapters/preview/local.ts`, Preview tests | **Defer** supported execution until workspace/generation/custody/output/cancellation gaps are qualified. No first-dogfood dependency. | Later scope |
| `src/product.ts`, `src/adapters/git/bootstrap.ts`, `src/adapters/git/project-config.ts`, `src/index.ts`, Product/package tests | **Adapt/replace** coupled root, role, Check-policy and bootstrap declarations. Existing repositories have a corpus; capture honest baseline assurance. Change readers/writers/config/exports coherently without live conversion or invented past acceptance. | SC-2/4/7 |
| `tests/kernel/invariants/architecture.test.mjs`, remaining scripts/tests | **Retain/adapt** purity, export/source closure, ownership and historical-state guards. Review allowlist changes; do not weaken tests to hide changes. Change/Gate/Work/Evidence suites are imported by architecture tests even where absent from direct globs. | Every source slice |

Defer full hosted UI, broad format/plugin ecosystems, optional external database connectors and delivery features until demanded by a bounded use case. No separate Skills/Check Pack/Archive/Sources ontology or second authoritative database belongs in the successor design. Historical converters are donor evidence, not ready migration tools.

## Current queue

Every source slice requires its design dependencies and a concrete authorized packet. SC-2A, the D4 review and SC-2R are complete at their recorded scopes. SC-2B was implemented locally; its subsequent quality review is recorded below and does not accept the implementation unchanged. No repair or subsequent integration slice is authorized by this review.

| ID | Work and dependencies | Exit / status |
| --- | --- | --- |
| SC-0 | Second-pass Wiki/plan hardening and history decluttering. | **Complete, docs only.** Prior ledger verified in exact Git history; envelopes/ownership, links, diagrams, decision coverage, safety boundaries, protected work and diagnostics checked. No new archive copy or runtime migration. |
| SC-1 | Software/non-software, historical and adversarial contract probes; compare good prose/Git with minimal structure. | **Desk round reviewed; SC-2A authorized.** Findings and the bounded successor packet are below. No independent fresh-Agent trial, measured quality/effort comparison or runtime semantic test ran. Remaining protocol decisions are explicitly deferred, not silently closed. |
| SC-2 | Native corpus foundation and an honest adoption boundary; depends on reviewed SC-1 decisions. | **SC-2A and SC-2R complete at bounded scope; SC-2B requires repair before acceptance.** Existing tests and diagnostics pass, but the SC-2B review found untested admission, boundary and native Store compatibility defects. Define the repair packet before source changes. Public material reads, scope/privacy enforcement and link interpretation remain deferred; actual adoption writes belong in SC-4. Discovery must not claim acceptance. Retain supported old-envelope fixtures; no live conversion. |
| SC-3 | Consequential evaluator and diff; depends on SC-1/2. | **Blocked.** Actual changed commitments/assumptions; scoped supported/contradicted/unresolved findings; no empty-policy, label/waiver, hidden-context or ranking bypass. Evidence can challenge current belief without silently overwriting Wiki. |
| SC-4 | Change transitions and durable Git-linked checkpoints, including initial baseline adoption; depends on SC-1–3. | **Blocked.** Close the adoption writer/receipt protocol before exposing promotion. Explicit adoption/revision/retirement/re-adoption, intent changes, exact baselines and authority; checkpointed ≠ accepted ≠ realized. Native edits outside the service do not invent acceptance. Preserve real outcomes through clone/restart and reject rewritten-subject/self-OID claims. |
| SC-5 | Planning, attempts and semantic joins; depends on SC-4. | **Blocked.** Shared maintenance obligations survive decomposition; assess actual joins. Route defects/path conflicts/intent revisions correctly. Enforce supported tool/effect scope and observe cancellation/custody; reconcile unknown effects. |
| SC-6 | Native historical information retrieval, durable judgments/custody and fresh-context continuation; depends on SC-3–5. | **Blocked.** Recover exact retired material and its disposition without an external connector, archive database or rerun model. Retrieval is not re-adoption. Preserve current obligations through context limits; show missing/private/stale inputs; reconcile restart cut points without duplicate effects. |
| SC-7 | Immutable external first-dogfood qualification; depends on SC-2–6. | **Blocked.** Frozen corpus/profile/budgets and real semantic feedback → inquiry/repair → accepted outcome → restart. Native Git/Wiki/Change loop works with zero external data connectors. Exclude unsupported Preview/format/hosting breadth; resolve actual release/controller authority and state compatibility. |

## SC-1 recommendations and remaining decisions

The maintainer reviewed these recommendations as the basis for SC-2A. They refine the agreed Wiki contracts but do not constitute a frozen wire schema or close the explicitly deferred decisions. Only the minimal read-only SC-2A profile is specified enough to bound the next source packet. The Git foundation, Change-controlled transitions and optional nature of external data connectors are not open alternatives.

| ID | Recommendation from the probes | Still to resolve / boundary |
| --- | --- | --- |
| P-O1 | Distinguish exact material identity, interpreted passage role, adoption disposition and evidential support. A file may contain several roles; no required per-paragraph schema or author-issued IDs. Change records can cite exact source passages and attributable interpretations. | Interpretation and cross-revision passage mapping remain fallible. Exact record fields wait for SC-3/4. SC-2A returns material only, not authoritative role labels. |
| P-O2 | Separate approval to pursue the next scope, adoption of specified knowledge effects, scoped realization and overall completion. Intermediate adoption needs review and authority for that exact effect; it does not finish its parent Change. | Freeze transition/receipt shapes in SC-4. The four loops may be concise, but an early finding cannot bypass scoped Review merely because it arose during Planning. |
| P-O3 | Amend an open Change with an explicit new intent revision when continuing the same inquiry; revisit Decision for material changes. Independently governable work or work after final disposition uses a linked Change. Preserve original intent and results. | The inquiry boundary requires judgment, not text similarity. Reassess affected plans, results and authorizations; an old approval never silently applies to the amended subject. |
| P-O4 | Retain exact basis/candidate, intent revision, interpreted effects, finding grounds, evidence/source versions, authority, disposition and remaining obligations as Change-associated records with prose explanations. | Select encoding, storage/ref layout, retention and write ordering before SC-4 writes. A receipt cannot name its own enclosing commit OID; adding a record must not silently change the assessed artifact scope. No new authoring primitive. |
| P-O5 | Assess the actual joined candidate. Preserve original judgments as historical evidence; do not transfer them to rewritten subjects merely because prose, labels or some tree bytes match. | Specify supported rewrite/reuse/retention rules before those operations are exposed. No blanket Git history-rewrite policy or branch-per-stage convention is selected here. |
| P-O6 | Binding discovers existing material. A later authorized initial Change can adopt an explicit current baseline interpretation and its declared assurance, effective now, without inventing earlier Changes or reviews. Discovery neither endorses all text nor denies known human practice. | Initial actor authority must not come from the candidate's own instructions. Bulk/scoped adoption and ambiguous-role handling need the SC-4 protocol; no implicit adoption in SC-2. |
| P-O7 | Start with exact tracked Markdown bytes, repository-relative identity and explicit limits. Link/heading labels aid navigation but are not cross-version identity or permission. Do not normalize source bytes or infer retirement from paths. | SC-2A's profile is below. Git/access binding, links, renames, Obsidian interpretation and history permissions need subsequent packets. No mandatory archive tree or live format conversion. |
| P-O8 | Interpret concrete effects, affected obligations and support before applying deterministic transition checks. Findings retain reasons, scope, assumptions and uncertainty. Encoding checks are not a semantic evaluator. | SC-3 must select an evaluation/coverage/disagreement method and test it with adversarial and independent cases. Known required assurance cannot be replaced by confidence, empty checks or a keyword match. |
| P-O9 | Select context from intent, affected paths, shared obligations and their grounds; retrieve additional native history/information when a question or counterexample demands it. Preserve source lineage and declare consequential omissions. | This is a retrieval strategy proposal, not a complete dependency oracle or measured advantage. Ranking, eviction and repeated summaries cannot remove obligations or create independent evidence. |
| P-O10 | Present discovery, proposals, assessments, authorizations and observed outcomes distinctly; expose only supported capabilities. Neither source content nor an integration grants authority. | Client/authorization contracts follow after the internal read boundary. No new public API, external connector, host integration or model executor in SC-2A. |
| P-O11 | Preserve exact-read, identity and purity mechanisms; replace special-root/Item coupling deliberately rather than weakening the old decoder in place. | D4 was reviewed and repaired under SC-2R; its failed original lane remains historical evidence. Native Git/material integration still requires its own packet. SC-2A remains a pure internal boundary independent of Store execution. |
| P-O12 | Keep this inquiry and the proposed pure source slice free of candidate execution, paid models and external data dependencies. | Execution profile, empirical quality/effort thresholds, numeric runtime budgets, immutable qualification and independent controller compatibility remain open for the slices that need them. |

## SC-1 desk probes and reasoning

**Method and limits.** These are synthetic examples with stipulated facts, reviewed by the parent assistant against Wiki. They are neither observations of this package nor executed tests, independent reviews or fresh-Agent continuation trials. Each outcome is conditional on its stated premises; real findings would need attributable interpretations and relevant observations. No Python phrase check is used as evidence of meaning.

### 1. Software: a bad join despite isolated success

- **Given:** adopted knowledge says “Authentication secrets must never enter logs, including diagnostics.” UI work adds `credentialBundle` to login requests; backend work forwards it; observability work logs complete failed-login request bodies. Suppose each worker reports passing isolated tests that never exercise the combined path.
- **Reasoning:** the combined path places credentials in a logged body, contradicting the shared obligation even if files merge cleanly. The counterexample is the data flow, not a missing keyword or a test count. Review cannot accept that join unchanged. Implementation can redact the field; Planning must coordinate the shared boundary and a joined-path check. The candidate cannot authorize its own waiver.
- **Minimal need:** preserve the shared obligation, the relevant producer/consumer relationship and the actual joined subject. A complete automatically inferred dependency graph is not established by this example.

### 2. Non-software: onboarding knowledge is not an outcome

- **Given:** a Change aims to halve onboarding time. Three interviews identify confusing instructions; a proposed guide addresses that confusion. There is no rollout observation or duration measurement. An optional connector may supply interviews, but the same inquiry can use authorized local material.
- **Reasoning:** the interviews can support a scoped claim about those participants and a credible trial, not a population-wide causal result. Review may adopt the guide as the intended procedure, leaving rollout and measured improvement unrealized. A commit alone does not adopt it. Planning needs a trial, responsibility, measurement conditions and privacy limits; Implementation records what happened; Review assesses the original outcome claim.
- **Counterexample:** changing the goal to “publish a guide” after producing one must revisit Decision as an explicit intent revision. It cannot turn the original goal into a realized outcome retrospectively.

### 3–5. Historical reasons: retain, reassess, reconsider

Use the same retained decision throughout: a team chose a managed service because a local alternative lacked a required export feature **and**, independently, the team lacked support capacity. Either reason was sufficient to reject the local option in that recorded context.

| Variant and given current observations | Reasoned outcome |
| --- | --- |
| Both conditions still hold. | The old grounds remain relevant; reuse them rather than inventing a new rationale. This does not prove that no other consequential condition has changed. Infrequent retrieval does not retire the decision. |
| The team gains support capacity; the export feature is still absent. | One ground is defeated; the other independently remains. Reassess affected claims without automatically reversing the conclusion or pretending the original judgment used today's evidence. |
| The local alternative also gains the required feature. | The recorded rejection grounds no longer rule it out. Reopen comparison of costs, migration risks and current goals; this makes the alternative a live option, not automatically the best option or an adopted decision. |

Native Git history must expose the exact old conclusion, grounds, conditions and later disposition. A newly accepted Change can revise the current conclusion while keeping the prior judgment historically attributable. Rerunning a model is not recovery of that original judgment.

### 6. Retirement is an intentional effect, not file housekeeping

- **Given:** an adopted obligation is moved unchanged into `archive/` within the tracked tree, then omitted from an Agent's prompt. No retirement has been accepted.
- **Reasoning:** neither operation retires it. If an authorized Change instead retires that obligation, its exact prior passage and retirement reasons remain discoverable in Git. That retirement need not retire unrelated assertions in the same file. Merely observing that text disappeared cannot fabricate the missing acceptance record.

### 7. Retrieval can inform or challenge without adopting

- **Given:** a query returns a retired rule and three summaries derived from the same source; generated synthesis recommends following the old rule. Its previous acceptance and retirement reasons are available.
- **Reasoning:** historical acceptance is not current endorsement; three derivative summaries are not three independent grounds. Retrieve the retirement context before reconsideration. A new observation may undermine a current assumption or block proposed work, but the generated answer cannot silently replace accepted knowledge. An explicit Change is still needed for re-adoption.

### 8. No connectors, unavailable history, restricted context

- **Given:** there are no external data connectors. Relevant material is in retained local Git history. In variants, the referenced object is unavailable, or the current actor lacks access to a source used by an earlier judgment.
- **Reasoning:** the native-history case must work without a connector. The other variants require an honest access/availability limit, not invented reconstruction or disclosure under obsolete permissions. An authorized reviewer may supply suitably scoped assurance; otherwise required unresolved assurance blocks acceptance. A context rank or an inaccessible source is not evidence that an obligation is irrelevant. Do not leak private contents or existence details while explaining the limit.

### 9. Existing repositories and mixed passage roles

- **Given:** a tracked `README.md` states an operating commitment, quotes a vendor's incompatible claim, and includes a proposed exception. Notes record an abandoned alternative. There are no CodeWiki envelopes, document IDs or historical CodeWiki adoption records.
- **Reasoning:** all are discoverable material; they do not acquire equal governing force. Interpreting the commitment, quotation, proposal and historical account is a contextual judgment, not an extension/path test. An authorized initial Change can accept an explicit current interpretation and declared assurance now. It cannot invent past acceptance, treat every sentence as policy, or let the proposed exception authorize its own adoption. Ambiguous consequential roles remain unresolved.

### 10. Exact subjects, intent amendments and interrupted review

- **Given:** work and an assessment refer to intent revision 1 and candidate A. Intent is materially amended to revision 2; the work is also rebased or joined with another candidate. A reviewer loses its response after an attempted acceptance write.
- **Reasoning:** preserve the old assessment as history, not approval of the new intent/subject. Assess the actual candidate and affected authority again; valid old observations may be reused only with an explicit account of applicability. Reconcile the possibly completed write before retrying an effect. A record cannot solve this by asserting its own enclosing commit hash, and a cache cannot reconstruct a lost original judgment.
- **Unclosed protocol issue:** assessment artifacts, acceptance records and resulting Git commits need an explicit subject-closure and write-ordering design before SC-4. This round identifies that requirement; it does not claim to have implemented or qualified it.

### Comparison: good prose/Git versus minimal structure

Use the **same facts and reasons** on both sides. Well-written Markdown can already explain every case above; Git can already preserve its exact versions. This desk exercise supplies no measured quality or effort advantage over that baseline.

A small machine-readable association is justified where it enables a concrete safeguard: binding an assessment to an exact intent/candidate, locating a passage, linking a retirement to its grounds, tracking a shared obligation across work, or exposing a missing required result. The reasons remain readable prose. Source identity, role, disposition and support must not collapse into one `accepted` or `confidence` flag. These are Change-associated records and derived views, not additional authoring primitives.

Incorrectly extracted relationships can still produce incorrect findings. Deterministic checks can reject malformed/stale bindings or enforce declared transition rules; they do not establish that the interpretation is faithful, complete or true. Independent semantic cases, fresh-context trials and measured effort remain future evidence. Do not expand this into compulsory metadata for every paragraph, a universal score, a Check Pack, an archive database or a mandatory vector index.

## Authorized SC-2A packet: plain Markdown material boundary

**Status: implemented and validated together with SC-2R; included in the reviewed combined checkpoint.** This is the first SC-2 increment, not completion of native Git discovery, adoption, semantic evaluation or Hub integration. The four-file scope below remains binding.

**Sequencing correction:** implementing “honest adoption” in SC-2 while deferring acceptance records to SC-4 was underspecified. SC-2 must provide material discovery and honest limits without asserting adoption. SC-4 must implement authorized initial and subsequent adoption on top of the evaluator and durable Change protocol. SC-3 can reason over explicitly supplied baselines in bounded fixtures; it cannot manufacture an operationally adopted baseline or expose unsupported promotion.

**Goal:** separate ordinary tracked Markdown material from the old canonical `WikiFile`/Item decoder. Start with a pure internal boundary over explicitly supplied snapshot/file descriptors and bytes. This avoids depending on the unaccepted D4 patch, adding an ad hoc Git connector, or silently widening an existing public reader's authority.

| Packet element | Authorized scope |
| --- | --- |
| Allowed source/test files | New `src/kernel/wiki/corpus.ts`; new `tests/kernel/wiki/corpus.test.mjs`; targeted source-closure assertions in `tests/kernel/invariants/architecture.test.mjs`; progress in this plan. No other file changes without a revised packet. Existing Wiki/Kernel ownership patterns already cover these locations. |
| Input/output | Explicit Git snapshot identity, repository-relative paths, file modes and original bytes supplied by the caller, with declared limits. Return bounded material records or typed input/limit/decoding issues. No adopted/retired/support judgment is inferred, and the operation does not replace an already known disposition with “unaccepted.” This is not a new persistent corpus store. |
| First format profile | Regular tracked-file descriptors (`100644`/`100755`) with `.md`/`.markdown` extensions, case-insensitive for that suffix only, at any supplied repository-relative path. UTF-8 text, including optional BOM, CRLF and non-NFC text; preserve original bytes and path spelling. No required envelope, ID or YAML parsing. Keep standard Markdown and Obsidian syntax as text; resolving their links is later work. |
| Scope and failure | Reject malformed descriptors, duplicate exact paths, unsafe absolute/traversal/NUL paths, paths not representable losslessly as UTF-8, invalid UTF-8 content and non-regular Markdown modes. Do not silently normalize, case-fold or conflate distinct paths. Report non-Markdown exclusions without calling the corpus complete knowledge. Do not dereference symlinks, submodules, imports or embedded instructions, inspect the working tree, or consult machine-local ignore/config files. Tracking and authorization are supplied upstream facts, not proven by this pure function. |
| Resource contract | Require positive safe-integer limits for examined entries, total path bytes, eligible document count, per-document bytes and total eligible bytes; no implicit unlimited default. Bound the descriptor scan and cumulative bytes before decoding/copying material, including a corpus dominated by non-Markdown entries. Adapter/transport admission must later enforce limits before loading bytes; this pure boundary cannot prove host containment or prevent upstream allocation. Concrete production defaults remain outside this packet. |
| Identity honesty | Validate identity shapes and internal descriptor consistency using existing primitives where suitable. Do not claim that a supplied OID authenticates its bytes. Native Git object/content checks, repository binding, once-resolved sources and current access authorization remain required adapter/service work before exposure. No self-asserted descriptor is an authority grant. |
| Acceptance cases | Ordinary `README.md` and nested Markdown without envelopes; optional frontmatter retained as data; BOM/CRLF/non-NFC and source-buffer ownership; duplicate/unsafe paths; exact and exceeded size/count boundaries; malformed UTF-8; executable-mode Markdown treated only as data; symlink/submodule rejection; non-Markdown and archive-named paths; existing envelope text preserved without automatic adoption. Tests assert these bounded behaviors, not prose meaning. |
| Purity and compatibility | No filesystem, Git/process/network/model execution, ambient time/randomness or mutable caches in the kernel. Preserve caller bytes and guard against later buffer alias mutation. Keep old readers, writers, fixtures, existing export barrels and public API unchanged; no dual authoritative reader/writer, migration, bootstrap, config/package/dependency edits or private data ingestion. New material helpers must not become an alternate acceptance path. |
| Validation after approval | Primary LSP diagnostics on touched source/tests; focused corpus tests plus the architecture suite and its imported regressions; native `npm test` with actual exit status; scoped diff and exact protected-work preservation review. Add the file to actual architecture closure rather than loosening its guards. No candidate pack/install, paid calls, D4 retry, activation or handoff. |
| Stop conditions | Required behavior touches D4, public APIs, authorization, the old codec or additional files; source identity cannot be represented honestly with current primitives; an architecture guard demands broader changes; a resource/encoding decision would silently rewrite data. Stop and amend the packet rather than expanding scope or weakening tests. |

### SC-2A internal representation decisions

- The synchronous internal entrypoint accepts a supplied snapshot `GitOid`, an array of descriptors and all five explicit limits. Each descriptor names `path`, `mode` and `oid`; eligible Markdown additionally requires `bytes`. Non-Markdown descriptors do not need content loading. Git OID syntax and one object format per snapshot are checked, not object type, custody, tracking, authorization or hash-to-byte authenticity.
- Material records contain frozen identity metadata, exact decoded `text` and `byteLength`, not a caller-owned byte view. Strict UTF-8 decoding preserves BOM, CRLF, non-NFC text and all other valid UTF-8 data; re-encoding must recover the exact admitted bytes. Inputs and results cannot alter one another through a shared returned buffer. Shared backing buffers, detached views and out-of-bounds views are rejected. A native counterexample showed that a detached view could otherwise decode as an empty document; the guard and regression test now distinguish it from legitimate empty content.
- Descriptors are passive caller-supplied data, not a JavaScript sandbox. Required fields and optional bytes must be own data properties; accessors are rejected without invocation. Extra caller metadata is not interpreted or copied into results. Paths use `/` separators; reject backslashes, drive prefixes, absolute paths, empty/dot/traversal segments, NUL and unpaired surrogates without rewriting them. Case and Unicode spelling otherwise remain exact. A segment trie rejects file/descendant collisions in either order, including collisions involving excluded symlinks or gitlinks, without quadratic prefix-string construction.
- Preflight entry/path/document/byte budgets before decoding any content. Each result list preserves its relative descriptor order; non-Markdown exclusions are reported separately. Failure returns one typed issue, never a partial success. Eligible files claiming the same OID must contain identical text/bytes; this consistency check still does not authenticate that OID.

After SC-2A, publish another bounded packet for native Git/service integration, scope/privacy and links, informed by a separate D4 review. Do not label the pure material boundary a working folderless Wiki or treat its native tests as semantic/runtime qualification.

## Bounded D4/baseline review

**Authorized scope:** review the three preserved D4 files, their native failure paths and the architecture restriction; update this plan only. Capture: `~/.local/state/codewiki/evidence/sc2-d4-review-npfka02s/`, covering all 196 tracked/output paths at the unchanged HEAD and branch above. All prior protected paths were verified before review. Native tests and narrowly derived diagnostic fixtures are evidence, not an executor/provider lane retry or accepted source changes. Any diagnostic fixture changes must be identified explicitly; neither hidden branches nor repaired fixtures count as passing the unchanged suite.

**Questions:** separate fixture/oracle defects from Store defects; check binding and error classification against the Wiki repository-binding contract; determine whether the new filesystem import belongs in the adapter's permitted dependencies; identify a bounded repair packet without weakening the Kernel boundary or hiding failures. D4 source, frozen read-purity tests, SC-2A source/tests, Wiki and unrelated work remain unchanged.

### Pre-repair findings and native evidence

Line references in this section refer to the captured pre-repair subject. SC-2R's resolution and passing evidence are recorded below; do not read these historical failures as new pending gates.

**Review disposition: do not accept D4 unchanged.** Preserve the direction of its single observed binding and composition, but repair the defects before a green combined-source checkpoint. Review alone does not waive failed tests or permit staging unrelated work.

| Finding | Cause and bounded evidence | Required disposition |
| --- | --- | --- |
| Invalid-root test stops during setup (`project-store.test.mjs:400`). | `mkdir(invalidNested/.git)` omits its missing parent. The Store is not invoked for this case or subsequent assertions. An external diagnostic variant adding recursive directory creation passed the entire invalid-root case. | Repair the fixture, retain its rejection and preservation assertions. Do not change the Store to accommodate failed setup. |
| Separate-dir seed oracle is inverted (`:475`). | `.then(() => true, error => error.code === 128)` returns `true` for the expected missing ref and can return `false` for unrelated errors. This failure is not evidence that the Store wrote the seed. A diagnostic variant uses `show-ref --verify --quiet`, accepts exit 1 only as absence and propagates other errors. | Replace the oracle with explicit Git outcomes; do not weaken the no-seed-write assertion. |
| Hidden linked-worktree test violates the existing ref contract (`:486–488`). | Once the seed oracle is corrected, the next failure is `invalid_ref`: `refs/heads/topic` is not admitted by `isManagedProjectRef`. A second variant supplies the independently observed `linkedHead` as an exact-OID selector. The full layout case then passes, including separate-dir writes, linked-worktree reads/writes and bare SHA-256 reads/writes. | Test binding through the supported exact-OID selector. Do not broaden the Kernel's managed-ref policy to repair a fixture. |
| Store failures are misclassified (`project-store.ts:440–456`; D4 test `:684–688`). | `#readOptionalRef` searches human-readable diagnostics for `status 1`, which also matches exits **12 and 128**. An unavailable bound `.git` therefore appears as `not_found`. `#assertCompleteCommit` collapses all failed type commands into `invalid_object`, including launch/exit failure, timeout and output limit. Native probes reproduced these results; the existing D4 test itself incorrectly endorses the ref-side `not_found`. Both classification bodies predate D4; the proposed binding patch exposes rather than repairs them. | Preserve process-failure categories and distinguish positively observed absence from unavailable observation. Correct the test's ref expectation as well as the Store, without changing true absent/non-commit object semantics. Inspect the same collapse in tree-mutation validation and the optional-ref helper's CAS callers. |
| Architecture rejects `node:fs` (`architecture.test.mjs:308–339`). | D4 adds only `lstatSync` and `realpathSync` for root observation; the adapter dependency list does not declare `node:fs`. Kernel purity is not the failing assertion. | Review a file-specific adapter dependency allowance plus exact read-only import assertions. Do not remove the guard, allow arbitrary dependencies, or relax Kernel purity. |

**Execution subjects and outcomes:** Node `v24.14.0`, Git `2.55.0`; preserved project source/test bytes bound by `state-before.json`. The unchanged Store, frozen read-purity and Local Server suites ran **24/27 passing**, three failures, no skips, exit **1**. The exact failing architecture case ran **0/1**, exit **1**. Thus the four original failures remain reproduced, not repaired. No full `npm test` rerun was needed to diagnose these scoped failures.

Diagnostic variants are explicitly separate evidence, not new project tests: the two-fixture-change variant ran **1/2**, exit **1**, exposing the hidden ref-policy failure; its exact-OID successor ran **1/1**, exit **0**, reaching the remaining layout assertions. Transformation manifests record every changed fixture/oracle expression and the three import relocations back to the actual source files. Their passing results do not make the unchanged suite pass.

The native error-taxonomy probe recorded seven cases, exited **0** after confirming the current counterexamples, and verified its fixture refs unchanged. Healthy absent-ref `show-ref --verify --hash` exits **128** on this Git profile; the quiet verification form exits **1**. Therefore replacing the substring with an exact `status 1` comparison is not a sufficient repair. An unavailable administrative directory also exits **128**, while the Store reports false absence for refs and `invalid_object` for object reads. Injected post-open exit 12 also becomes false ref absence; timeout and output overflow retain their category on the ref path but become `invalid_object` on the object path. These are diagnostics of incorrect behavior, not seven correctness passes or evidence of a foreign-repository read. No retargeted read or ref mutation was observed.

### Per-hunk disposition and limits

- **Retain direction, conditional on repair:** the shared invocation profile, captured administrative-directory binding, runtime-readonly observed object format and Local Server consumption of that same observation. The Local Server's independent config-based probe/default-to-SHA-1 path should not return. Its removal and error-union adjustment remain part of the reviewed D4 candidate, not a new public API expansion under this review.
- **Retain useful tests, repair fixtures:** asynchronous Git setup with scrubbed inherited Git variables, positive/negative root and format cases, explicit process bounds, restoration of adversarial environment and no-write assertions. Previously unreachable assertions are not coverage until actually reached.
- **Adapt classification deliberately:** carry structured command outcomes rather than inferring status from diagnostic text. A failed observation is not proof that a ref/object is absent. Preserve timeout/output-limit errors, existing exact-selector behavior, authorization/request-digest checks, expected-old-OID CAS and unknown-effect reconciliation. This review did not execute a CAS failure-injection matrix or settle every command protocol.
- **No stronger assurance:** these local fixtures do not prove physical custody, concurrent-replacement resistance, filesystem-call deadlines, process-tree containment, hostile Git-binary authenticity or operational qualification. The synchronous filesystem observations and per-process budgets must not be described as a demonstrated total host-containment bound.

### Authorized SC-2R packet: repair baseline observation and fixtures

**Status: implemented, reviewed and natively validated.** Repair edits were confined to `src/adapters/git/project-store.ts`, `tests/adapters/git/project-store.test.mjs`, `tests/kernel/invariants/architecture.test.mjs` and this plan. Preserve the existing Local Server D4 bytes, both SC-2A corpus files, frozen read-purity tests, `.pi/settings.json`, `outputs/`, Wiki, lifecycle history and all other work. Preserve the pre-repair D4 diff in the review capture; do not reset or transplant it.

| Repair element | Bounded contract |
| --- | --- |
| Fixture/oracle repairs | Create the invalid-root fixture parent; use an absence oracle that propagates unexpected failures; select the linked worktree's independently observed exact OID. Keep the no-write, correct-subject, bare-store and ref-admission assertions. |
| Command/result classification | Preserve structured exit/launch/timeout/limit outcomes in the private adapter boundary. Before coding, choose and record Git command protocols on the supported profile that distinguish absent refs/objects from failed observation; a diagnostic substring or exact exit-128 check is insufficient. Do not introduce fallback discovery or change the public issue-code catalog. Propagate command failures through commit/object preflight, tree-mutation type validation and optional-ref/CAS consumers. |
| Dependency declaration | Permit `node:fs` only for this Store module, with an explicit assertion that the imported APIs are the reviewed `lstatSync` and `realpathSync`. Keep source/test closure, other adapter allowlists and the Kernel's no-ambient-effects boundary intact. |
| Required regressions | Healthy unborn/absent ref; healthy missing or wrong-type object; unavailable/moved administrative state; nonzero exits including 12 and 128; launch failure, timeout, excessive output and malformed response. Verify direct read and relevant write/CAS preflight failure categories without claiming unobserved effects did not occur. Keep same observed object-format/location binding and no retarget/no-write checks. |
| Validation and checkpoint | Fresh primary diagnostics; original Store, frozen read-purity, Local Server, corpus and architecture suites with no hidden fixture failures; typecheck/build/full `npm test`, actual exit codes and exact source bindings. Review the final combined diff and protected bytes before selecting owned paths for a checkpoint. Corrected diagnostic variants are not substitutes for the unchanged tests becoming correct and passing in source. |
| Stop conditions | Needs Kernel/ref-policy changes, public API/port/schema expansion, weakened authorization/digest/CAS rules, new persistence/controller machinery, edits beyond the four-file repair scope, or unsupported Git protocols without an explicit profile decision. Amend the packet first. No old-lane/provider retry, paid model call, candidate pack/install, activation, handoff or push. |

#### SC-2R protocol and preservation decisions

Before source edits, `~/.local/state/codewiki/evidence/sc2r-repair-k90fgynt/` captured all 196 tracked/output paths, the pre-repair diff, an empty index and unchanged HEAD/branch/refs. All 195 non-plan paths matched the D4 review capture. Only the four authorized repair paths may change; all 192 other paths remain byte/mode protected.

Native disposable Git **2.55.0** probes selected these protocols for this tested profile:

- Ref existence: `show-ref --exists <exact-managed-ref>` returns exit **2** with empty stdout for absence, **0** with empty stdout for presence, and other exits for failure. Malformed ref content returns 1; an unavailable repository returns 128. Preserve numeric exit status and stdout emptiness in the private failure type. Unsupported protocol exits fail typed; do not fall back to diagnostic-text parsing or claim compatibility with untested Git versions.
- Ref resolution: after confirmed presence, `show-ref --verify --hash` captures the exact OID once. A dangling symbolic ref or a disappearance between the two observations is a failure, not fabricated absence. Validate one exact OID line. Existence plus resolution is not an atomic observation; expected-old CAS still guards writes.
- Object type: `cat-file --batch-check=%(objectname) %(objecttype)` receives one validated exact OID on stdin. Only a successful, exact `<oid> missing\n` is absence; successful commit/tree/blob/tag records report type. Nonzero exit, launch failure, signal, timeout, excessive output, malformed/extra text or a different OID remain observation failures. Use the same private helper for commit and tree-mutation preflight.
- Failed CAS: reconcile observed new values without blindly retrying. If desired values are not observed, preserve the original command failure; classify a nonzero-exit conflict as stale only after successfully observing divergence from expected values. Do not infer no effects from a failed response or label unchanged refs as stale merely because a command failed.

#### SC-2R implementation and validation result

- Fixed all three fixture/oracle defects in the original test file, including the previously hidden linked-worktree selector problem. Ref admission remains unchanged.
- The private process failure union now retains numeric exit status and stdout emptiness. Ref existence uses the selected protocol; resolution and binary-writer OID records require exact UTF-8/OID lines. Leading BOM, extra lines and malformed responses are not normalized into success.
- One object-type helper serves commit, base-tree and mutation preflights. Timeout, limit, launch, signal and nonzero-exit failures propagate instead of becoming `invalid_object`. Adjacent commit-tree checks and post-write verification paths use the same distinction; a failed verification does not claim that no object was created.
- CAS preserves original failures when desired results cannot be reconciled; a successful observation of divergence is required for stale classification after nonzero exit. Tests verify unchanged-ref failures, real competing writes, applied-then-error reconciliation, no blind retry and applied-but-unreadable effects remaining unresolved.
- `node:fs` is allowed only for `src/adapters/git/project-store.ts`, and an AST assertion restricts that module to the reviewed `lstatSync`/`realpathSync` imports, rejecting namespace/default imports or additional re-exports. Kernel purity and other dependency allowlists remain intact.

Evidence in `~/.local/state/codewiki/evidence/sc2r-repair-k90fgynt/`: focused native validation passed **148/148**, exit **0**; after the final base-tree cases, full `npm test` passed **367/367**, exit **0**, with **0** skipped/cancelled tests. Typecheck and build completed successfully through that command's `&&` pipeline. Original Store, frozen read-purity, Local Server, corpus and architecture tests all ran in the full suite. Primary diagnostics on touched source/tests and the plan are clean; all-session diagnostics are checked separately. Logs, statuses, exact source bindings, the pre-repair diff and final checkpoint evidence are retained outside project Git.

Only the four authorized paths changed during repair. All **192 other captured paths** retain their exact bytes/modes, including Local Server's reviewed D4 bytes, both corpus files, frozen tests, Wiki, lifecycle history, settings and outputs. The combined checkpoint selects seven already-reviewed paths: this plan; `src/adapters/git/project-store.ts`; `src/adapters/git/local-server.ts`; `src/kernel/wiki/corpus.ts`; `tests/adapters/git/project-store.test.mjs`; `tests/kernel/wiki/corpus.test.mjs`; and `tests/kernel/invariants/architecture.test.mjs`. Staging the unchanged Local Server/corpus bytes incorporates their previously reviewed work, not additional repair edits. User settings and outputs are excluded. Git and external checkpoint evidence supply the resulting commit identity; the plan does not assert its own enclosing OID.

This is parent-reviewed native contract/structural validation on the recorded local profile, not independent semantic evaluation, physical-custody/containment proof, candidate qualification or activation. The earlier failed evidence remains attributable to its original subject. This prerequisite repair does not complete SC-2 or implement native Markdown integration.

## Validation and next action

SC-0's completed mechanical documentation checks and preservation counts remain at `0165f1b6cfe20751354114d4a6e9c20445092fb4:SEMANTIC_KERNEL_PLAN.md` and in its private evidence directory. They establish the checked structural/preservation conditions, not semantic correctness.

SC-1's pre-implementation mechanical review covered seven local links, 34 then-existing source references, cited Git object availability, whitespace and exact preservation of all 193 other captured paths. Its prospective corpus files now exist through SC-2A. That review remains separate from conditional desk reasoning, not independent semantic evaluation.

SC-2A native results, with logs and exact file bindings in `~/.local/state/codewiki/evidence/sc2a-corpus-68j90228/`:

- Final corpus suite: **25/25 passed**. Five explicitly selected architecture assertions also passed: source/test allowlists, Kernel boundaries, the internal corpus dependency closure, unchanged public reachability and unique native ownership. These selected checks do not replace the failing full architecture suite.
- Pre-LSP-edit full `npm test`: typecheck and build passed; **330/334 tests passed, four failed**, with no skipped/cancelled tests. The failures are in untouched `tests/adapters/git/project-store.test.mjs` at lines 365, 446 and 635, plus the unchanged architecture assertion rejecting the untouched D4 store's `node:fs` import. The three Store failures are fixture-directory `ENOENT`, separate-Git-directory repository identity, and vanished-administration classification (`invalid_object` rather than `command_failed`). This run did not validate or accept D4; its files and the architecture import restriction were not changed to make the suite green.
- Final scope/preservation review covers only the four authorized paths, all 192 other captured paths, frozen read-purity bytes, unchanged HEAD/branch/refs, an empty index and scoped whitespace/reference checks. Evidence is native contract/structural testing and parent code review, not independent semantic evaluation, host containment, operational discovery/adoption or release qualification.
- **Primary LSP passed after a pi restart.** Native tools became visible again; the precise cause of their prior absence was not established. The initial four-file primary check was clean. Auxiliary findings were **one error, three warnings, ten hints and one typo information item**; the earlier four-warning report incorrectly counted a style hint. The error and three warnings were addressed in scope, and post-edit typecheck plus 25/25 corpus tests passed. Intentional decoder-boundary hints and the combining-accent fixture spelling remain distinct from blocking errors. The D4 review then ran a fresh seven-file primary batch with **zero diagnostics** and checked the all-session error cache, rather than inferring session cleanliness from a turn delta. No pi-lens file, setting or rule was edited.

Next: after the reviewed combined checkpoint, define a bounded native Git/material integration packet using the now-validated Store and internal corpus boundary. Exact snapshot/source binding, admission limits before byte loading, scope/privacy and link treatment need explicit choices; reading material must remain distinct from adoption. No integration implementation is authorized by completing SC-2R. Provider/connector setup, candidate pack/install, live conversion, qualification, activation, handoff and push remain outside this round. Record another bounded packet here before any broader source work. Before eventual qualification, freeze the corpus/environment/profile and numeric time, memory/output, latency, token/cost/retry, omission/false-acceptance and intervention limits; binding safety failures cannot be averaged away.

### Authorized SC-2B packet: native Git material read integration

**Status: implemented locally, reviewed with unresolved defects, not checkpointed.** Current branch is `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`; working tree has unrelated `.pi/settings.json` modifications and untracked `outputs/`, both protected and excluded. This packet implements a read-only native material path from an exact resolved Git snapshot into the existing pure Markdown corpus boundary. The later quality review below supersedes any implication that green existing tests established the packet's full acceptance. It does not implement adoption, semantic evaluation, persistence of a corpus, public release qualification or controller handoff.

| Packet element | Authorized scope |
| --- | --- |
| Allowed source/test files | `src/server/queries/source.ts`, new `src/server/queries/material-source.ts`, new or existing tests under `tests/server/queries/` for source/material loading, targeted architecture assertions if import/ownership closure requires them, and this plan. If a small API contract type is unavoidable, stop and amend before editing `src/api/**`. No command, lifecycle, old Wiki codec, bootstrap, product config, package or Local Server changes. |
| Entrypoint shape | Add an internal server-query helper that resolves a supplied `ProjectSourceSelector` through `ProjectStorePort`, reads the exact tree for that commit, filters/adapts tree descriptors, loads bytes only for eligible Markdown regular files within explicit limits, then calls `readMarkdownCorpus`. Existing `loadWikiSource` and public Wiki reads remain envelope/Item reads and must not switch to material discovery in this packet. |
| Source binding | Resolve `canonical`/`change`/`commit` through existing `resolveProjectSource`; pass repository id, object format and exact commit OID to Store operations. The corpus `snapshot` must be the resolved commit OID. Verify the material read remains bound to that exact commit; a failed Store read is an availability/source issue, not evidence of absent knowledge. |
| Admission limits before bytes | Extend only internal read configuration if needed with explicit material limits: maximum tree entries examined, total path bytes, material document count, per-document bytes and total material bytes. Apply tree-entry and path/document budgets before blob reads; apply per-document and total byte budgets before accepting/copying each blob. Do not add implicit unlimited defaults or read every blob before rejection. |
| Descriptor treatment | Use `readTree` records as passive descriptors. Preserve repository-relative path spelling, mode and OID. Treat only `100644`/`100755` `.md`/`.markdown` paths as eligible content; report other paths as corpus exclusions via the existing pure boundary. Symlinks/gitlinks and file/descendant collisions must still be rejected by `readMarkdownCorpus`, not silently traversed. Do not consult ignore files, working tree state, filesystem paths, external connectors or local config. |
| Scope/privacy | This internal helper returns exact material text to authorized server code only. It must not expose a new public API, infer actor authority from repository content, leak unavailable/private source details beyond existing issue shapes, or treat discovery as current acceptance. Existing authorization checks for public queries remain unchanged. |
| Links and roles | Preserve Markdown, frontmatter, wiki links, Obsidian syntax, headings and old envelopes as text only. Link resolution, role interpretation, adoption/retirement/support judgments, passage identity and cross-revision mapping remain later SC-2/SC-3/SC-4 work. |
| Failure mapping | Map Store and corpus failures to a bounded internal issue type or existing project-source issue without expanding public issue catalogs unless separately authorized. Keep invalid limits/input distinct from unavailable Git observations. No diagnostic-string parsing or hidden fallback discovery. |
| Required regressions | Exact commit binding; canonical/change/commit selector resolution through existing Store fixtures or fakes; non-Markdown exclusion without blob read; Markdown blob read with preserved BOM/CRLF/non-NFC text; per-document/total byte boundaries; tree-entry/path/document limits before loading extra blobs; Store tree/blob failure propagation; symlink/gitlink Markdown rejection; duplicate/path-collision rejection; no adoption/current-knowledge flags in returned material. |
| Validation | Run primary LSP diagnostics on touched source/tests, focused material integration tests, relevant architecture assertions if edited, then `npm test` or the smallest plan-approved gate if the implementation remains test-local. Record actual exit statuses here. Use `lens_diagnostics mode=all` before declaring completion. No pack/install, paid model, provider setup, activation, handoff, live conversion or push. |
| Stop conditions | Need public API exposure, authorization-policy change, adoption writer/receipt, semantic evaluator, link graph, object-authentication beyond Store, broader config/product defaults, filesystem/Git calls outside `ProjectStorePort`, new persistence, or edits outside listed files. Stop and amend this plan first. |

Implementation note: prefer a private/internal helper and fake `ProjectStorePort` tests over widening user-visible capabilities. If production defaults cannot be chosen honestly without product/config work, keep the helper parameterized by explicit limits and defer defaults.

Validation recorded 2026-09-10: primary LSP diagnostics on `src/server/queries/material-source.ts`, `src/server/queries/source.ts`, `tests/server/queries/source-material.test.mjs`, and `tests/kernel/invariants/architecture.test.mjs` reported 0 diagnostics. Focused material tests passed with `node --test tests/server/queries/source-material.test.mjs` (6/6). Focused architecture assertions passed with `node --test tests/kernel/invariants/architecture.test.mjs --test-name-pattern="source and test file allowlists|corpus remains internal|source graph is closed"` (64/64, filtered run still enumerated all tests under Node's reporter). Full `npm test` passed after typecheck/build/smoke (`373/373`, exit 0). `lens_diagnostics mode=all` reported no issues across touched files before this validation note; rerun after plan edit before completion.

### SC-2B quality review: unchanged source, repair required

The maintainer requested verification before proceeding. This native parent review changed only this plan; it did not repair source, rewrite tests, checkpoint, fetch/push, load a controller, install a candidate or invoke paid execution. Evidence is retained outside project Git at `~/.local/state/codewiki/evidence/sc2b-review-9bauezhx/`: the initial 197-path hash/mode capture, scoped pre-review patch, exact copies of the two untracked implementation/test files, diagnostic script and results, disposable Git fixtures, and full-suite log/status. The subject is the worktree captured at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`, not that commit alone; the two new files are uncommitted.

**Disposition: retain the bounded internal direction, but do not accept SC-2B unchanged.** The helper has no production caller, as confirmed by LSP references and the unchanged public-reachability architecture guard. It invokes no writer, does not parse frontmatter or resolve links, and does not assign adoption or current-knowledge status. Those are useful boundaries, not proof of complete source admission or authorization for future exposure.

| Finding | Evidence and consequence | Required next disposition |
| --- | --- | --- |
| Invalid limits are checked after I/O (`material-source.ts:26–34,39–50,79–81`). | A numeric `NaN` in `maximumDocuments` or `maximumPathBytes` bypasses the comparisons, loads a Markdown blob, and only then fails in the pure corpus reader. The result is `invalid_project_state`, not an invalid-request distinction. Path budgeting also allocates a full encoded path before testing its size. | Validate and snapshot all five explicit limits before Store work. Use bounded path accounting and preserve the distinction between malformed input and failed observation; do not describe the current reader as enforcing every admission budget before allocation. |
| A legitimate empty file fails after an exact byte-budget fill (`material-source.ts:61–64`). | A real Git fixture with `a.md = "1234"`, empty `b.md`, and a four-byte total budget fails `limit_exceeded`. The pure corpus accepts both. A fake with the same entries in reversed order succeeds. Zero remaining bytes is not proof that another document requires positive bytes. | Define a bounded empty-blob observation compatible with the Store's positive read cap, without silently widening the corpus budget or adding an unbounded read. Add inclusive-boundary and empty-file-order regressions. |
| Store availability categories are discarded (`material-source.ts:86–90`). | Injected `timeout` and `command_failed` outcomes from both tree and blob reads become `invalid_project_state`, indistinguishable by code from invalid limits/content. The new test endorses this collapse. The source-resolution helper already has a similar narrower mapping; the review does not silently authorize changing public issue catalogs. | Select an internal typed failure/cause contract that keeps input, source availability and source validation distinct. Preserve structured causes instead of requiring callers to parse diagnostic strings. |
| The reused Store is not compatible with all admitted Markdown paths (`project-store.ts:184–185,859–864`). | Real tracked `Cafe\u0301.md` and `guide[1].md` list successfully but fail blob admission under the older NFC/glob-character path policy. A tree containing `U+E000.md` and `U+10000.md` is rejected because Git's UTF-8 order differs from JavaScript UTF-16 string order. These Store constraints predate SC-2B; fake-only integration tests miss the incompatibility. | Explicitly scope a read-path/order repair or narrow the supported material profile with an honest issue. Prefer preserving the already chosen exact-path corpus contract; do not silently normalize paths, broaden writer permissions or patch the adapter outside an amended packet. |
| Required regression coverage and status were overstated. | The six new tests use only a fake Store. They do not cover tree failure/drift, entry-limit enforcement, Gitlink Markdown, exact duplicates, invalid limits or real-Git filename behavior. The prior command put `--test-name-pattern` after the file operand; its 64-test result was an unfiltered architecture run with imported tests, not evidence of the claimed selection. The top-level current entry/queue also still described SC-2B as future work. | Add the missing packet cases and native Git integration fixtures in the repair. Keep the useful full-run evidence, correct the selection claim, and keep one consistent current status. |

Fresh validation on Node **v24.14.0** and Git **2.55.0**: primary LSP diagnostics for the new reader/test, architecture test, Store, corpus and source resolver were clean. Unchanged focused material/corpus suites passed **31/31**, exit **0**. Full `npm test` passed **373/373**, exit **0**, with **0** skipped/cancelled tests, including typecheck/build. The external diagnostic script exited **0** after asserting **14 observations**, including the counterexamples above and a positive native read; these are confirmations of current behavior, not 14 correctness passes. No throughput, token-saving, semantic-quality or operational-qualification benchmark ran.

The repair should retain exact commit/OID binding, the pure corpus boundary and the unchanged public reader. Resolve admission and native Store compatibility before optimizing. Potential later optimizations are a once-validated snapshot session, bounded batched Git reads and rebuildable revision/profile-bound passage/metadata indexes; the current Store repeats object-type and full closure checks per blob. None is implemented or authorized by this review. Retrieval must remain authorization-aware, preserve source/disposition context and report consequential omissions; cache or ranking decisions cannot retire obligations or adopt source claims.

### Session boundary and agreed direction

The maintainer agreed that Git and OKF/frontmatter are complementary, not competing replacements. Git supplies native object lookup, trees, revision history and refs; CodeWiki supplies bounded access, exact-source binding, typed failures and governed knowledge interpretation. Avoid rebuilding version control or imposing accidental read-path restrictions. Redundant closure walks and repeated path-to-OID lookups are optimization candidates, not permission to remove required completeness or failure checks.

Ordinary Markdown remains valid material without mandatory metadata. An optional OKF profile can supply interoperable concept metadata and navigation, with conformance rules applied to documents opting into that profile. Git history does not establish factual truth, and frontmatter status/verification claims do not authenticate authorization or adoption. Rebuildable indexes and bounded passage retrieval may reconcile these layers without a second authoritative store. The current reader does not implement OKF interpretation; existing Wiki envelopes remain unchanged. No format migration, public exposure or retrieval implementation is authorized by this discussion.

This is a suitable fresh-session boundary: review is complete and repair has not begun. Re-read the current entry, verify branch `audit/clean-baseline`, HEAD `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`, index and worktree, and preserve all uncommitted work, especially user settings and outputs. First propose a bounded SC-2B repair packet covering pre-I/O admission, empty-file byte boundaries, typed availability failures, the native Store read-path/order contract and missing fake/real-Git regressions. Do not interpret the fresh session or this handoff as repair approval. After an authorized repair, validation and reviewed checkpoint, request explicit authority for proposal-branch synchronization; canonical `main` movement, release qualification and controller handoff are separate decisions.

### Proposed SC-2B-R packet: admission and native read compatibility

**Status: proposed, not authorized or implemented; held for the subsequent knowledge-navigation clarification below.** The proposal session was limited to verification and proposing the repair. Its assumption that the optional-profile policy was settled is reopened; the technical repair findings remain recorded, not accepted. Approval of a revalidated packet is required before source/test edits; a fresh session, PM delegation decision or green historical suite is not that approval. SC-2B remains unaccepted and uncheckpointed.

**Verified subject and preservation.** Native Git reports branch `audit/clean-baseline`, HEAD `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`, and no staged changes. The worktree contains modified `.pi/settings.json`, this plan and `tests/kernel/invariants/architecture.test.mjs`; untracked `src/server/queries/material-source.ts`, `tests/server/queries/source-material.test.mjs`, and four existing output documents. A fresh 197-path hash/mode capture, uncommitted-file backups, worktree/index patches and comparison to the pre-review capture are retained at `~/.local/state/codewiki/evidence/sc2b-repair-packet-3x0kzyri/`. All 196 non-plan paths match the review capture; branch, HEAD and the complete ref set also match. The plan differs because it contains the subsequent quality review and handoff. Node remains `v24.14.0`; Git remains `2.55.0`. No source test or diagnostic probe has been rerun to claim repair success.

**Goal and profile decision.** Preserve the chosen exact-path Markdown corpus contract rather than narrow it to the Store's writer-oriented NFC/glob restrictions. Git remains the native object/tree/ref/history substrate. Ordinary Markdown needs no metadata; optional OKF/frontmatter remains complementary data, not an authority or adoption mechanism. This repair does not parse OKF, resolve links, build indexes or create another storage system.

| Packet element | Proposed bounded contract |
| --- | --- |
| Exact edit scope after approval | `src/server/queries/material-source.ts`; `src/server/queries/source.ts` only for shared lossless source resolution with legacy mapping preserved; `src/adapters/git/project-store.ts` only for read-path admission and `readTree` ordering; `tests/server/queries/source-material.test.mjs`; `tests/adapters/git/project-store.test.mjs`; targeted closure/compatibility assertions in `tests/kernel/invariants/architecture.test.mjs`; and this plan. No new files or edits elsewhere without amendment. |
| Pre-I/O admission | Snapshot all five limits as own data fields before the first Store call or `await`. Reject missing/accessor/non-numeric fields, NaN, infinity, fractions, zero, negatives and unsafe integers as typed invalid input, with zero Store calls. Subsequent caller mutation cannot alter admitted limits. This is passive-data validation, not a sandbox for hostile JavaScript proxies. No implicit defaults. |
| Tree/path/document admission | Check returned entry count independently of Store compliance before scanning or loading blobs. Count lossless UTF-8 path bytes against the remaining budget without allocating a full encoded copy first. Enforce document count before blob loading; retain strict descriptor/path/mode/OID consistency, duplicate and file/descendant rejection. Do not normalize or case-fold paths. Retain the unchanged pure corpus as the final content/descriptor validation boundary; shared Kernel-helper extraction would require amendment rather than an unlisted edit. |
| Inclusive content boundaries | With positive remaining bytes, request the smaller of the per-document cap and remaining total. At zero remaining bytes, request a cap of exactly one byte to distinguish an empty blob under the Store's positive-cap contract. Accept only a zero-length result; a nonempty response or Store overflow is a typed limit failure. Recheck every returned byte length before retaining it, even if a fake or faulty Store ignored its cap. Do not decode/copy over-budget content or return partial success. This explicit one-byte observation allowance is not an increase in admitted corpus bytes; Store process/output bounds remain separate from corpus limits and do not prove exact host-allocation containment. |
| Internal typed failure contract | Give the material helper a discriminated internal failure/cause contract: invalid caller input, source selection/binding failure, Store failure and corpus validation failure remain distinguishable. Preserve the original structured `ProjectStoreIssue` code and operation for snapshot, tree and blob failures, including `timeout`, `command_failed`, `incomplete_object`, `not_found` and `limit_exceeded`; retain structured corpus codes/locations as well. Do not infer absence from failed observation or parse messages. Refactor source resolution into one shared detailed internal path; keep existing `resolveProjectSource` as a compatibility wrapper returning its current result/error shape and mapping. The material helper uses the detailed path, without resolving refs twice or duplicating selector policy. No Store port, public issue catalog, wire response, export barrel or authorization change; detailed causes must not leak through legacy public responses. |
| Native read-path policy | Split read admission from `validRepositoryPath`'s mutation policy. Exact reads and literal tree prefixes admit lossless UTF-8 repository-relative paths under the corpus's safety grammar, including non-NFC text, literal glob characters and valid control-character filename data. Reject absolute/drive, NUL, backslash, empty/dot/traversal segments and malformed Unicode without rewriting. Do not carry writer-only NFC, glob, reserved-name or 4,096-byte portability restrictions into reads; bound observation with the existing finite Store process/output profile and explicit material budgets. Keep NUL-delimited Git protocols, exact returned-path matching, literal pathspecs, exact OIDs and no filesystem dereference. Native process/platform limits remain honest typed failures, not universal filename/host-support claims. |
| Native tree order and writer fence | Validate recursive `ls-tree -r -z` output using Git's UTF-8 byte ordering rather than JavaScript UTF-16 ordering. Preserve native output order and reject malformed, duplicate or out-of-order records; do not sort broken responses into validity. Include nested directory/prefix cases against native Git as the oracle. Preserve writer admission, mutation ordering, private writer-side base-tree handling, request digests, authority checks and CAS unchanged; the read repair does not promise writers can mutate every readable path. Any writer-side ordering defect requires another packet. |
| Exact-source and purity fence | Resolve the supplied selector once, then retain repository id, observed object format, exact commit and blob OID checks throughout. Keep completeness checks and bounded failures; no closure-walk optimization, batch session, fallback discovery, fetching or new cache. Never consult working-tree bytes, ignore files, external connectors or document instructions for authority. Preserve old envelope reads and existing access checks; this helper still has no public material-read caller and claims no adoption. |
| Protected work | Preserve the frozen `tests/adapters/git/local-read-purity.test.mjs`, `src/adapters/git/local-server.ts`, both SC-2A corpus files, user settings, four outputs, `AGENTS.md`, Wiki, lifecycle history and all other unrelated paths byte-for-byte. Extend the existing uncommitted SC-2B implementation/tests in place only after approval. Keep their pre-repair bytes in external evidence; no reset, stash, transplant, competing checkout or automatic staging. |

#### Required regression matrix after approval

1. **Admission and boundaries:** every invalid value class across all five limits yields no snapshot/tree/blob calls; own-data/accessor checks and mutation during an awaited Store call cannot bypass the limit snapshot. Cover exact/exceeded entry, UTF-8 path, document, per-document and total byte budgets, non-Markdown-heavy trees, a Store returning too many entries or oversized blob bytes, and empty files before/after exact total exhaustion. After exhaustion, both one-byte and larger nonempty blobs must fail without partial success.
2. **Source and failures:** canonical/change/exact-commit selection uses one resolution and exact subsequent requests. Exercise snapshot/tree/blob Store failures with retained typed causes, tree-commit and blob-OID drift, repository/object-format mismatch, invalid selectors, and unchanged legacy resolver error shapes. Availability failure must not become invalid input or confirmed absence.
3. **Material validation:** exact duplicate paths, file/descendant collisions in both orders, symlink and gitlink Markdown, unsafe/malformed paths, malformed UTF-8 content, executable-mode text, non-Markdown exclusions without content reads, BOM/CRLF/non-NFC bytes, and preserved old envelopes/frontmatter/links with no interpreted adoption fields.
4. **Real Git, not fake-only coverage:** disposable external fixtures using isolated Git configuration on the recorded Node/Git profile, with SHA-1 and SHA-256 cases. Read ordinary Markdown, `Cafe\u0301.md`, `guide[1].md`, other literal glob/pathspec-looking names, tab/newline names, and `U+E000.md` versus `U+10000.md`; include nested prefixes and exact empty-file boundaries. Compare exact names, OIDs, byte content and order against native Git, not normalized/sorted test expectations. Malformed output/order and non-lossless names require explicit negative fixtures. Assert relevant native read purity and retain narrower writer rejection cases without changing writer policy.

#### Execution, validation and acceptance boundary

The PM owns scope, preservation, evidence and acceptance synthesis. No delegation is needed for this proposal. After approval, use one writer in this checkout; any delegated implementation receives this exact packet. A fresh-context read-only review follows the completed diff, with no concurrent writers. Tooling/provider/lane failure is a blocker to report, not permission to switch execution protocols. No paid model calls are authorized.

Before implementation, reverify the exact subject and capture any intervening work; record approval and any resolved amendments here. After implementation, run primary LSP diagnostics on the touched source/tests before builds, then focused material/Store/corpus tests and the frozen read-purity/Local Server regressions. Run the full architecture suite and full `npm test` with actual exit statuses, typecheck/build results and pass/fail/skip counts. If using `--test-name-pattern`, place it before file operands and record the actual selected tests rather than infer selection from an unfiltered total. Finish with `lens_diagnostics mode=all`, scoped whitespace/diff review and a fresh hash/mode/index/ref comparison. Keep logs and exact source bindings outside project Git. Existing 373/373 and the 14 diagnostic observations remain historical evidence, not repaired acceptance.

Acceptance requires every reproduced defect to have a regression, every gate to pass on the final exact worktree subject, no unresolved blocking review finding, unchanged public behavior outside the deliberate native-read repair, and verified protected bytes. Only then consider a reviewed native checkpoint of owned paths, excluding settings/outputs. This is native contract validation, not semantic evaluation or release qualification.

**Stop conditions:** approval is absent; the subject or protected work changed unexpectedly; the repair requires additional files, public API/port/schema changes, broader authorization, writer-policy changes, Kernel changes, link/OKF interpretation, default/config changes or removal of completeness checks; or a required native protocol cannot preserve the declared exactness/bounds. Stop and amend rather than narrow the contract silently or weaken tests. No push/fetch, canonical-ref movement, migration, candidate pack/install, mutable controller execution, activation or controller handoff is in this packet. Proposal-branch synchronization still requires later explicit authority.

### Prior research handoff: Project boundary and knowledge navigation

**Maintainer clarification.** The intended boundary is the Git-managed Project, not an artificial knowledge boundary at `.codewiki/`. That does not imply abandoning linked concepts, frontmatter or OKF where they produce clear navigation/efficiency gains. `.codewiki` may still have configuration and trace responsibilities. Whether metadata is mandatory for curated knowledge, which conventions it uses, and what an agent should see remain open design questions. This clarification reopens the earlier inference that removing the directory boundary settled an optional-OKF policy; preserve the earlier entries as history, not as authority to bypass this clarification. No directories, lifecycle records or runtime state are being moved. Existing source, Wiki and lifecycle boundaries remain operational until an approved design/change packet replaces them.

**Read-only research observations, not adopted requirements:**

- Obsidian stores optional note properties in YAML frontmatter and supports text, lists, numbers, booleans, dates, tags and aliases. Ordinary Markdown/wikilinks and backlinks supply navigation independently of those fields; aliases also support link suggestions and unlinked-mention discovery. Nested properties are not supported by the normal Properties UI. JSON frontmatter is read but saved as YAML, which creates a concrete compatibility issue for the current CodeWiki canonical-JSON decoder. Format resemblance is not proof of lossless editor round-tripping. Sources: [Properties](https://obsidian.md/help/properties), [Aliases](https://obsidian.md/help/aliases), [Internal links](https://obsidian.md/help/links), [Backlinks](https://obsidian.md/help/plugins/backlinks).
- The canonical OKF home has moved from `GoogleCloudPlatform/knowledge-catalog/okf` to `GoogleCloudPlatform/open-knowledge-format`; the former README explicitly marks its copy frozen. The inspected [v0.2 specification](https://github.com/GoogleCloudPlatform/open-knowledge-format/blob/0b87c52c6ef999286c745e19998fdfcd03d5dbee/SPEC.md), especially sections 3, 4, 6 and 11, requires YAML frontmatter with nonempty `type` for every non-reserved Markdown concept in a conformant bundle. `index.md` and `log.md` are reserved; concept identity is path-based; relationships use standard Markdown links. Other metadata families are optional, and trust tiers are advisory rather than access control. Declaring a mixed Project to be an OKF bundle would impose obligations on README files and ordinary `index.md`/`log.md` files too. Project-wide discovery and full-bundle conformance must not be conflated or silently narrowed to a partial-conformance claim.
- [Google's introduction](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing/) reports Knowledge Catalog ingestion support and provides producer/visualizer examples explicitly described as proofs of concept. Independent starter/guide projects also exist, including [this handbook](https://supachai-j.github.io/open-knowledge-format-starter/en/index.html) and [DSOM's adoption guide](https://linuxmalaysia.github.io/deep-state-of-mind-for-my-ai/OKF-ADOPTION-GUIDE/). These establish activity and implementations, not independently measured production success or a CodeWiki efficiency benefit. DSOM labels its 98% token-reduction figure an engineering target, and some of its v0.2 field examples differ from the canonical spec. Search-provider failures limited the broader adoption search; lack of located evidence is not proof of no successful deployments.
- [Marksman](https://github.com/artempyanykh/marksman) already provides Markdown heading symbols, link completion/hover, definition/reference lookup, rename and diagnostics. Its [feature documentation](https://github.com/artempyanykh/marksman/blob/main/docs/features.md) describes repository-root workspaces, configurable filename/title resolution and ignore-file handling. Reuse is worth evaluating before inventing a knowledge navigation protocol. However, a live filesystem/LSP workspace is not automatically the exact Git snapshot or tracked-file set required by CodeWiki. Syntactic link resolution also does not infer authoritative semantic relationships from prose. [RepoGraph](https://arxiv.org/abs/2410.14684) and [SWE-agent](https://arxiv.org/abs/2405.15793) supply relevant evidence that repository structure and agent interfaces can help coding tasks, not direct proof for OKF or this knowledge corpus.
- Hashline supplies short read/edit anchors to reduce edit-targeting friction; it does not resolve concepts or replace exact source identity. The [original benchmark report](https://blog.can.ac/2026/02/12/the-harness-problem/) uses mechanical React mutations and author-run comparisons, not knowledge-navigation tasks; results vary across models. [Prime-compatible Hashline extensions](https://github.com/tickernelz/pi-hashline-prime) exist, but implementation and recovery behavior vary. The inspected [Prime Agent RLM documentation](https://github.com/PrimeIntellect-ai/prime-agent/blob/main/packages/coding-agent/docs/rlm.md) describes programmatic file operations through `ipython`, not a mandatory built-in Hashline protocol. Do not transfer one implementation's benchmark to another, or treat short edit tags as cryptographic, durable or cross-revision passage IDs.

**Proposed decision/evaluation work, still not implementation authority.** Separate four questions: (1) Project-wide material scope; (2) concept/alias/link and frontmatter requirements for curated knowledge; (3) bounded agent navigation and retrieval; (4) exact passage/edit addressing. Compare strict Project-wide OKF with a mixed Project containing explicitly declared, potentially mandatory-format curated concepts and a conformant exchange boundary. A metadata-based concept distinction need not recreate a privileged folder. Neither option is selected yet. Consider Markdown links as the portable baseline, explicit handling of Obsidian extensions, and source-bound definition/reference/neighbor lookup before building a new protocol or index store.

Clarify "inline replacement" before specifying it: inserting links, showing temporary annotations/tooltips, and recursively expanding definitions are different behaviors. Prefer testing read-only, provenance-bearing annotations and bounded on-demand definitions first; automatic source rewrites, ambiguous alias resolution and recursive context expansion risk semantic drift, cycles and token inflation. Any source rewrite should be a separately reviewed change. Suggested links must remain distinguishable from authored links and adopted assertions.

A future evaluation should hold Project subject, tasks, model and budget fixed; compare plain Markdown/search, explicit links/backlinks, added metadata, and LSP-style navigation, with inline presentation as a separate factor. Test Hashline separately on edits so navigation and editing effects are not confounded. Include ambiguous terms, contradictory/historical claims, missing links, renamed files/headings, excluded material, stale snapshots and multi-hop questions. Measure correct supported answers, citation/retrieval precision, successful updates, token/tool/latency cost, wrong-link and stale-source failures, and ongoing metadata/link maintenance effort. Define cases and acceptance criteria before running; no paid model experiment, benchmark execution, package install, source change or migration was authorized in this research turn.

**Preservation and next step.** Branch/HEAD remain `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`; index and refs are unchanged and all 196 non-plan paths still match the repair-proposal capture. Research-turn preservation evidence and the pre-update plan are under `~/.local/state/codewiki/evidence/knowledge-navigation-research-2_bt580f/`. Only this plan was updated. SC-2B-R remains paused; discuss and approve the metadata/navigation direction before revalidating its scope. Do not edit the Wiki to make an unresolved research recommendation look accepted.

### Prior research handoff: structured Wiki, synthesis, intake and item types

**Status: continued brainstorming and research, not a design packet or implementation approval.** The maintainer agreed that Markdown should express current meaning, Changes preserve justification/evolution and Git preserve exact revisions. An index was clarified as an optional tool implementation detail, not another agreed primitive. The maintainer now proposes a uniform minimum knowledge representation in body/frontmatter, a standard synthesis/consolidation step, direct-authored documents entering the same Change intake, and reconsideration of a predictable managed Wiki directory and item types. These questions remain open; neither the earlier folderless interpretation nor this new recommendation authorizes a migration or source edit. The maintainer's sentence beginning "What's interested is that changes are" was unfinished and must not be completed by inference.

**Research and historical findings:**

- [Karpathy's LLM Wiki idea](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) closely matches the proposed synthesis pattern: retained sources, a persistent interlinked Wiki and conventions for incremental maintenance, rather than re-synthesizing sources for every query. It is explicitly an idea/personal-workflow description, not controlled evidence of efficiency, semantic correctness or negligible maintenance cost. CodeWiki need not adopt its LLM-exclusive writer, automatic answer promotion, duplicate log or index requirements.
- Evidence does not justify runtime-independent efficiency guarantees. [Evaluating AGENTS.md, v2](https://arxiv.org/abs/2602.11988v2) reports no general task-success improvement and over 20% average inference-cost increase in its context-file evaluations, with caveats including a Python-heavy task distribution. This is not a direct test of a Change-governed Wiki. Conversely, [ACE, v3](https://arxiv.org/abs/2510.04618v3) reports gains from structured incremental context maintenance in its tested agent/domain tasks, while identifying brevity bias and context collapse from repeated rewriting. These studies test different systems; together they support evaluating useful, nonredundant project-specific context and preservation of detail, not either blanket rejection or guaranteed benefit. The target should be minimum sufficient meaning, not minimum token count.
- [OKF v0.2](https://github.com/GoogleCloudPlatform/open-knowledge-format/blob/0b87c52c6ef999286c745e19998fdfcd03d5dbee/SPEC.md) offers a small common envelope with required `type`, extensible unknown types and structural Markdown guidance, but no mandatory body sections. A uniform CodeWiki body would be an additional declared profile, not something OKF already guarantees. [Diátaxis](https://diataxis.fr/) supports choosing forms by reader need rather than one universal body template; it does not establish a CodeWiki item taxonomy or agent-performance result.
- [FHS purpose](https://refspecs.linuxfoundation.org/FHS_3.0/fhs/ch01.html) explicitly includes predictable locations for users/software and coordination across parties. [Git's repository layout](https://git-scm.com/docs/gitrepository-layout) distinguishes administrative storage from working-tree content and supports explicit layout variants. A predictable managed Wiki home is therefore a defensible convention, not a contradiction of a Project-wide evidence/context boundary. Neither analogy proves a particular directory name or an automatic semantic authority rule.
- [Git status](https://git-scm.com/docs/git-status) reports tracked/index/worktree differences and untracked paths; ignored files need explicit observation. It does not interpret intent, start an agent, authorize disclosure, classify a Change or accept knowledge. A normal commit can bypass an application pipeline unless acceptance is separately enforced at the controlled boundary. Detection, proposal association, assessment, acceptance and realization must remain distinguishable.
- Item types were not experimentally rejected. At `b7ea18bc8`, the Wiki explicitly described `codewiki.wiki:definition` and `codewiki.wiki:claim`, with guards against a Definition for every noun or a Claim for every sentence. Pivot `9b24f5a74` said an exhaustive taxonomy had not been selected; `e85874937` removed the dedicated type sections during the broad implicit-corpus rewrite. Current `src/kernel/wiki/item.ts` still requires a namespaced `itemType` through `WikiItemEnvelope` and `decodeWikiItemEnvelope`. Future taxonomy was reopened/left unspecified, while executable type-bearing envelopes remain. This history does not require restoration of the old envelope, ID scheme or full architecture.

**Assistant recommendation for discussion, not an adopted specification:** distinguish a Project-wide source/context boundary from one predictable home for consolidated, current accepted Wiki knowledge. Preserve the existing sources and direct-authored drafts; synthesis proposes an attributable knowledge delta rather than silently rewriting source evidence or generating a parallel copy of every document. Sources and already structured user documents use the same admission/assessment contract; an already adequate document can yield a no-op transformation. Keep one authoritative home for each governed meaning rather than independent conflicting summaries. No special archive, new persistent index or competing source database is implied.

Use a small shared document shape plus purpose-specific sections. Candidates worth comparing are Definition, Reference/entity description, Contract/constraint and Procedure, with independently governed Claims only when their separate lifecycle is useful. These are proposed roles, not a closed mandatory enum; subject kinds and document roles should not be conflated. Put project-specific meaning, applicability, important exceptions, uncertainty and relationships near the text; retain detailed rationale, input bindings, assessments and authority in Changes. A type label cannot downgrade obligations evident in the actual content. Avoid copying universal textbook knowledge merely to populate a template.

Direct authoring should be discoverable as unassessed work and associated with a new or already owning Change, not automatically staged, uploaded, overwritten or accepted. Preserve the author's exact draft and make synthesis diffs reviewable. Missing intent, conflicting sources or unavailable assessment can leave a proposal pending. Validate the actual synthesized/reconciled candidate, including stale-source and baseline drift, rather than transferring approval from earlier draft bytes. Do not require one Change per keystroke/checkpoint or re-summarize the entire Wiki on every intake. Every accepted transition of managed knowledge is governed; external reality, temporary files and every Git commit are not thereby controlled.

"Guarantee alignment" needs a bounded meaning: the system can enforce explicit machine-checkable invariants and required recorded assessment/authority at a controlled acceptance boundary. It cannot guarantee faithful synthesis, complete relevant-context discovery or semantic alignment of arbitrary natural language. Unsupported or unresolved interpretation remains visible; an LLM's claimed alignment and a passing format check are not proof. Preserve the existing formal-invariant versus semantic-evidence distinction.

**Preservation and continuation.** Branch/HEAD remain `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`; all 196 non-plan paths match the previous research capture, with index and refs unchanged. This turn only updates the plan. The pre-update plan and preservation capture are under `~/.local/state/codewiki/evidence/structured-wiki-research-y__04t07/`. No source/build/test experiment, paid model evaluation, install, push, migration, activation or controller handoff occurred. SC-2B-R remains paused. Discuss the representation/home/intake choices and finish the maintainer's incomplete thought before drafting an acceptance packet or changing Wiki design truth.

### Latest agreed conceptual continuation: Change-originated Wiki and hot/cold lineage

**Confirmed direction, not implementation approval.** The maintainer completed the earlier thought: Changes are the single governed creation/evolution point for Wiki knowledge and the reusable document forms needed to express it. A Change makes intent and its contextual consequences explicit in natural language, rather than generating documentation only after implementation. The maintainer then confirmed that Wiki must not duplicate session conversations. Runtime sessions, transcripts, compaction and temporary working context belong to the agent runtime. Change transforms noisy brainstorm/source input into attributable, structured knowledge supporting committed intent; Wiki stores its accepted consequences and evolves through subsequent Changes. Existing types can be reused, and useful new types can themselves be proposed through Changes; this does not select a taxonomy or require a new type/page for every Change.

**Responsibility split:**

- Runtime owns session mechanics and temporary execution context.
- Change preserves intent, relevant assumptions, concise attributable rationale, alternatives that matter, exact inputs/assessments and recorded outcomes. It is not a transcript mirror or a requirement to store private model reasoning.
- Wiki expresses minimum sufficient current Project meaning, including essential applicability, exceptions, uncertainty and links to deeper grounds. It is not a retrospective session summary or an outcome regenerated by rerunning inference.
- Git preserves exact accepted and historical representations and their lineage. No new authoritative index, archive database or duplicated source corpus is implied.

**Hot/cold relationship and retained safeguards.** The maintainer identifies hot/cold paths as the cleanup mechanism and requires superseded/stale knowledge to retain its links to the Changes that created it. Interpret hot Wiki as accepted consequences that still apply, whether their originating Changes are ongoing or completed: an active unaccepted proposal is not governing knowledge, and completion alone does not make a superseded consequence current. Accepted normative targets can precede realization, but that gap must stay explicit.

Cleanup is governed semantic evolution, not runtime context eviction or age-based deletion. A subsequent accepted Change can revise, consolidate or retire knowledge and remove obsolete detail from active guidance while preserving exact prior text, relevant grounds and disposition through retained Git history. Stale support flags reassessment; it does not silently revoke a still-applicable obligation. Distinguish stale, contradicted, superseded and retired rather than treating them as one automatic archive state. Reading cold material cannot re-adopt it; re-adoption requires a new accepted scoped transition.

Retain many-to-many lineage: an Item or passage can be established and revised by several Changes; one Change can affect several Items. History should expose creating/adopting, revising, superseding/retiring and re-adopting Changes where recorded, not merely the most recent editor. Keep that relationship recoverable when old content leaves the hot Wiki, without requiring a permanent duplicate historical page in the active tree. Exact linkage/identity representation remains a design question. For pre-existing knowledge without recorded origins, retain honest observed-baseline provenance rather than fabricating historical Changes.

**Open design, unchanged authority.** The agreed relationship constrains future profile, type, intake and retention design, but does not settle folder naming, envelope/schema, type-definition bootstrap, passage identity or cleanup protocol. Natural-language synthesis and alignment remain fallible; the existing exact-subject, authority and bounded-assurance requirements still apply. No source edit, Wiki rewrite, migration, acceptance API expansion or execution/paid-model experiment is authorized. SC-2B-R stays paused pending a revalidated bounded packet and approval.

Branch/HEAD remain `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`. The 197-path capture and pre-update plan are under `~/.local/state/codewiki/evidence/wiki-lifecycle-principle-nndioheh/`; all 196 non-plan paths, index and refs match the prior capture. Only this plan is updated to preserve the conceptual handoff; source/tests, settings, outputs, Wiki and lifecycle records remain untouched.

### Wiki representation draft W1: corpus, envelope and types

**Requested deliverable, not execution authority.** This draft makes the agreed conceptual direction concrete for review. It does not alter existing Wiki files, the canonical-JSON decoder, Change records, configuration or package APIs. The YAML below is a proposed representation, not a claim of compatibility with current executable contracts. The six core categories and fixed Wiki/type-definition homes with optional domain folders have since been agreed. Required fields, exact profile/body contracts and runtime mechanisms still require design acceptance before implementation. Example Change names and paths are illustrative, not existing lifecycle records.

#### Corpus layout

The agreed structural homes are `.codewiki/wiki/` for managed knowledge and `.codewiki/wiki/types/` for TypeDefinition documents, within the Git-managed Project. Sources and other Project files need not move there or adopt the Wiki format. Other folders such as `project/`, `system/` or `fieldwork/` are optional domain groupings proposed through Changes, not required scaffolding. A small corpus may keep content directly under the Wiki root. Its document type is a profile declaration, not an inference from its directory. The following is illustrative, not a scaffold of mandatory empty directories:

```text
project/
├── src/                         # Executable material, when applicable
├── docs/                        # Existing source/reference documents
├── README.md                    # Remains an ordinary Project document
└── .codewiki/
    ├── <configuration>          # Configuration format is not selected here
    ├── changes/<change-id>/     # Governed intent, grounds and lifecycle records
    └── wiki/                    # Consolidated knowledge at the selected revision
        ├── types/
        │   ├── definition.md
        │   ├── entity.md
        │   ├── contract.md
        │   ├── procedure.md
        │   ├── claim.md
        │   └── type-definition.md
        ├── project/
        │   └── vocabulary.md
        └── system/
            ├── external-effect.md
            ├── retry-safety.md
            └── recovery.md
```

There is no required archive, session, index or generated-view directory. `index.md` can later be an optional navigation artifact using OKF's reserved-file rules; it is not a new authoritative primitive or a mandatory part of W1. Runtime sessions/scratch remain outside source truth. A governed retirement can remove an Item from the current tree while retaining its exact prior versions and lineage through reachable Git history. Location alone does not establish acceptance, and sources outside this home remain relevant to assessment.

#### Proposed common frontmatter

For accepted Wiki Items, propose four required fields and optional navigation/evidence fields:

| Field | W1 proposal | Purpose |
| --- | --- | --- |
| `type` | Required string | An accepted core or Project-defined type. |
| `title` | Required string | Human-readable subject/title, also used by the first body heading. |
| `codewiki-origin` | Required nonempty list of Project-contained Change-path references | Creating/adopting origins retained across revisions; multiple origins support consolidation. These are not every historical editor or every source citation. |
| `codewiki-revision` | Required Change-path reference | The Change responsible for the current accepted Item revision. |
| `description` | Optional one-sentence summary | A bounded preview; requiring it universally needs justification beyond this example. |
| `aliases`, `tags` | Optional lists | Useful navigation vocabulary, not authority. |
| `sources` | Optional OKF-shaped source entries | Direct citations when needed; detailed input bindings and assessments remain in Changes. |

The `codewiki-*` keys are proposed flat profile extensions, deliberately avoiding a large nested frontmatter object. Change references are paths relative to the Item, not the enclosing Git commit's self-referential hash. Their records retain the authoritative exact subject bindings and complete history. A proposed frontmatter pointer does not authenticate acceptance or realization by itself. Snapshot plus path identifies the exact material; this draft adds no compulsory UUID, per-line hash or durable concept-ID scheme. Rename/split/merge continuity must be recorded by the relevant Changes, not guessed from filenames.

The origin/revision split avoids an ever-growing event log in each Item while preserving entry points into many-to-many lineage. Passage-specific grounds belong in explicit citations or independently useful Claim Items; document-level origins must not be misrepresented as exact attribution for every sentence. Baseline adoption records an honest observed origin where earlier provenance is unknown. Profile/type interpretation must be bound to the accepted snapshot, not whatever type document happens to exist later.

Example at `.codewiki/wiki/system/retry-safety.md`:

```markdown
---
type: Contract
title: Retry safety
description: Unknown external outcomes must be reconciled before retrying.
codewiki-origin:
  - ../../changes/establish-retry-safety/
codewiki-revision: ../../changes/clarify-unknown-outcomes/
aliases:
  - Safe retry
---

# Retry safety

A timeout does not establish that an external effect failed.

## Scope

This contract applies to Project-managed operations that can change external state.

## Rules

- Do not repeat an operation while its previous outcome is unknown.
- Reconcile the outcome before deciding whether another attempt is safe.

## Exceptions

A repeat is permitted when an applicable, verified idempotency contract makes it safe.

## Related knowledge

- [External effect](external-effect.md)
- [Recovery](recovery.md)
```

This is an illustrative policy, not a newly accepted obligation. Detailed intent, alternatives and assessments remain in the referenced Changes. The example carries no copied `approved`, `verified`, `active`, creation timestamp or full history log; optional imported OKF trust/lifecycle fields remain data, not CodeWiki authority.

#### Body contract and type catalogue

Every Item starts with its title and concise primary meaning. Applicability, essential exceptions, uncertainty and relationships must remain explicit where needed for correct interpretation. Omit genuinely unnecessary sections rather than filling templates with empty boilerplate. A type determines useful additional structure; one universal body template would conflate definitions, obligations and operational instructions.

The maintainer has agreed to W1's six core Item-type categories below. This is conceptual agreement to the catalogue, not implementation approval or acceptance of every suggested field/body section. The catalogue does not enumerate every possible future Project type:

| Core type | Knowledge represented | Suggested body sections after the primary meaning | Examples |
| --- | --- | --- | --- |
| `Definition` | The meaning of a term or concept in this Project | Meaning; scope; distinctions; related concepts | Change, activation, realized outcome, terminology. |
| `Entity` | A named thing, actor or system element and its relevant characteristics | Role/responsibility; boundaries; relationships; realization caveats where relevant | Project, component, service, actor, resource, data model, interface. |
| `Contract` | Obligations or constraints that must hold within a scope | Scope; rules; exceptions; observable assessment criteria where useful | Requirement, invariant, policy, interface guarantee, compatibility rule. |
| `Procedure` | How to perform a scoped activity | Preconditions; steps; expected outcome; failure/recovery handling | Runbook, recovery, release workflow, operational playbook. |
| `Claim` | An assertion requiring independently useful grounds or lifecycle | Assertion; scope; grounds; uncertainty/counterevidence | Observation, assumption, hypothesis, research finding. A hypothesis remains labeled as such; acceptance is not proof of truth. |
| `TypeDefinition` | A reusable Project-specific document form | Purpose; use/avoid conditions; relationship to core forms; required content; interpretation/validation limits | System Component, API Contract, Recovery Procedure. |

Entity kinds and document purposes are related but not interchangeable. Project-specific forms should reuse an existing core form unless a new distinction materially improves synthesis, interpretation or checks. For example, `System Component` can refine `Entity`, `API Contract` can refine `Contract`, and `Recovery Procedure` can refine `Procedure`. These are possible specializations, not additional required built-ins. Type names remain extensible under OKF; unknown external types can be read as material, but adoption as a CodeWiki-governed type requires the appropriate accepted definition. Parsing a type never executes code or grants authority.

Example type-definition body for a future `.codewiki/wiki/types/system-component.md`, using the same common frontmatter with `type: TypeDefinition`:

```markdown
# System Component

A System Component is a bounded part of the Project's system with a distinct responsibility.

## Use when

The component has responsibilities and interfaces that readers must reason about independently.

## Form

This type refines Entity. It does not create a separate lifecycle or acceptance mechanism.

## Required content

- Responsibility and explicit non-responsibilities.
- Interfaces and dependencies relevant to that responsibility.
- Applicable contracts.
- Any distinction between intended design and observed realization.

## Avoid

Do not create a component page for every source file or helper function.
```

A Change may propose this definition and Items using it, but an existing accepted base contract must govern that proposal; a type cannot grant itself acceptance or waive existing obligations. The initial `TypeDefinition` bootstrap and exact machine-readable validation mechanism are later design questions, not an implicit dynamic schema/plugin system introduced by W1.

**Not additional Wiki Item types:** sessions/transcripts belong to the runtime; intent, decisions, alternatives, implementation plans/tasks and assessment records belong to Changes; raw sources stay sources unless their substantive knowledge is consolidated; indexes/backlinks/graphs are optional views; draft, stale, superseded and retired are states/dispositions, not types. Current consequences of decisions do belong in appropriate Wiki Items. A Claim is not required for every sentence, a Definition for every noun, or a TypeDefinition for every Change.

**Status and preservation.** W1's six core Item-type categories are agreed conceptually. The fixed Wiki/type-definition homes and optional domain folders are also agreed. The four-field envelope, exact body contracts and ownership/access mechanisms remain proposals; no migration or source implementation follows automatically. The pre-update plan and 197-path capture are under `~/.local/state/codewiki/evidence/wiki-profile-draft-pve7f5wg/`. Branch/HEAD remain `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`; all 196 non-plan paths, index and refs match the prior capture. Only this plan is updated; SC-2B-R remains paused.

### Current exploration: agent-maintained knowledge ledger and general Projects

The maintainer agrees with the six Wiki Item types and now asks whether Wiki should be an agent-optimized knowledge ledger rather than a document collection users directly access/edit. The motivation is a stronger, predictable structural contract with enough freedom for non-software Projects. This ownership/access model is exploratory, not accepted policy. Do not interpret it as permission to deny access, introduce a service/database, alter package APIs, lock files or migrate existing content.

The assistant's proposed distinction for discussion is **user-governed, agent-maintained and inspectable**. Users express intent, supply sources and review consequences; agents propose structured knowledge; the trusted acceptance boundary validates and publishes the exact candidate. Generic runtime read/edit tools may still operate on authorized snapshots and drafts. The value comes from a single governed write path and a stable representation, not from secrecy or giving an agent unchecked authority. Human review should be able to inspect/export exact accepted text and changes, not rely exclusively on another model-generated summary. A convention against direct editing is not OS/service access control; an actor with repository write access can modify bytes or refs, and any stronger guarantee needs an explicitly controlled boundary.

The maintainer has clarified that "knowledge ledger" means authoritative recorded knowledge, not a second chronological event log duplicating Changes and Git. Hot Wiki retains applicable accepted meaning; Changes retain transition grounds; exact historic representations remain on the cold path. Strict prevention of direct reads would require a deliberately designed mediated access surface, whereas an agent-oriented on-disk format alone does not require proprietary query/edit mechanics.

Potential constraint layers are: (1) a small universal envelope and accepted-transition/provenance invariants; (2) versioned Project-defined type/body profiles evolved through Changes; and (3) enough natural-language freedom to preserve domain meaning, exceptions and uncertainty. Project-specific types cannot silently waive core acceptance rules, and schema validity does not guarantee semantic alignment. "Agent-optimized" does not establish one optimal representation across every model, runtime or domain; changes to W1 fields remain subject to design and evaluation.

Examples must now include non-software Projects. In a field research Project, Definition can define an observation category; Entity can describe a study site; Contract can specify required observation metadata; Procedure can describe a survey method; Claim can preserve a qualified finding; TypeDefinition can describe a reusable Field Observation form. Software source trees, APIs, compilation and tests are domain examples, not universal assumptions about Projects or realization.

#### Follow-up exploration: structure, type location and on-demand disclosure

The maintainer asks whether core bodies should be rigid and custom TypeDefinitions open, where types should be declared, whether storage should be flat or hierarchical, and whether agents and UI/TUI consumers should disclose knowledge on demand. These remain design questions, not authorization to add read APIs, renderers, access restrictions or runtime machinery.

The assistant recommends one strict common envelope and provenance/acceptance contract across every Item, stable content obligations for each core type, and governed extensions rather than an unvalidated custom-type escape hatch. A custom type should initially refine one core content type and add useful requirements; avoid deep/multiple inheritance until justified. Preserve room for extra prose, exceptions and uncertainty. A complete set of headings is mechanically checkable; adequacy or semantic alignment of their content is not thereby proven. Unknown imported fields/types remain readable source data, not automatically adopted CodeWiki semantics.

Use frontmatter `type` as the Item's discriminator. Type-definition Items themselves carry `type: TypeDefinition` and describe the named type's purpose, core relationship, required content and limits. The proposed `types/` directory is their predictable home; no second authoritative registry or executable plugin is implied. Exact custom-definition fields and validation syntax are still draft. Interpreting a historical Item requires the profile/type definition from its accepted source snapshot, not a mutable latest definition.

Prefer a hybrid physical layout: fixed Wiki root and type-definition location, with shallow, domain-oriented folders for content. A small corpus may remain flat. Neither a directory per Change nor a directory per document type is required; type and navigation semantics come from accepted content, not path placement alone. Scope, naming and link-update rules should be predictable without forcing every Project into a software-specific tree. This is a navigation hypothesis, not a measured universal performance result.

Recommend bounded progressive disclosure: snapshot overview/type/domain catalogue, candidate descriptors, selected Item/section, relevant links, then Change grounds or cold history when requested. A catalogue can be derived from accepted files; this does not reinstate a mandatory authored `index.md` or a new authoritative index. Missing optional descriptions should not be silently replaced with authoritative-sounding generated summaries. Agent and UI/TUI adapters should consume one source-bound logical read contract while remaining free to present it differently. This means owning consistent knowledge resolution/provenance semantics, not replacing generic runtime search/edit tools or introducing a proprietary query language. No transport/server/package choice is selected here.

Each navigation chain must retain its selected accepted revision; apply authorization/scope before discovery and follow-up reads, bound/truncate responses honestly, distinguish current/proposed/historical or unresolved material, and expose exact source locations. Progressive disclosure controls what is loaded, not what is accepted or retained. It must not perform cleanup, re-adoption, source rewriting or conversation management as a side effect. The hot/cold distinction remains semantic applicability/history, not a cache eviction policy.

The latest pre-update plan and 197-path capture are under `~/.local/state/codewiki/evidence/wiki-navigation-discussion-v30m1c88/`; all 196 non-plan paths, index, refs and HEAD match the preceding capture. Only this plan is updated. Earlier preservation evidence follows.

Only this plan records the agreed catalogue and unresolved ownership discussion. The pre-update plan and 197-path capture are under `~/.local/state/codewiki/evidence/wiki-ownership-discussion-30t22k0f/`; all 196 non-plan paths, index, refs and HEAD match the preceding capture. Source/tests, Wiki, lifecycle history, settings and outputs remain untouched. SC-2B-R remains paused; no source/schema/migration/access-control change is authorized.

### W1-DOC: bounded desired-state alignment

**Authority and scope.** The maintainer's agreement to the six types, clarification of structural versus optional folders, and instruction to proceed authorize this conservative documentation alignment. It is not a source repair, wire-profile freeze, corpus conversion or controller action. Apply accepted conceptual outcomes to desired-state prose; leave unresolved representations explicitly in this plan rather than presenting them as implemented or approved.

**Exact edit scope:** `SEMANTIC_KERNEL_PLAN.md` and the body text of `.codewiki/wiki/items/system/components/wiki.md`, `.codewiki/wiki/items/system/components/project.md`, and `.codewiki/wiki/items/system/components/change-trace.md`. No new files or directories. Preserve the three documents' existing canonical-JSON frontmatter byte-for-byte and the Project repository-identity/binding safeguard block byte-for-byte. All `src/**`, `tests/**`, `.pi/settings.json`, existing outputs, other Wiki documents, `.codewiki/changes/**`, `AGENTS.md`, index and refs are protected.

**Changes to document:** distinguish Project-wide sources from structured accepted Wiki; specify the two structural Wiki paths and optional domain grouping; record the six core categories without freezing YAML keys or dynamic schema machinery; establish Change-mediated synthesis/direct-authoring intake and many-to-many hot/cold lineage; preserve native Git retention, exact outcomes, applicability/uncertainty and semantic-assurance limits; keep runtime sessions separate from Change grounds. Do not introduce a new index, registry, archive, mandatory UI or proprietary query mechanism.

**Validation:** read the three bodies and their linked Project/Change boundaries, inspect the scoped diff for accidental normative expansion, run Markdown diagnostics and whitespace checks, validate relative body links, and compare all 197 captured path hashes/modes plus branch, HEAD, index and refs. Assert exact preservation of all three envelopes and the Project safeguard. No build, source tests or paid model evaluation is needed to claim this documentation-only result. Its checks are not evidence of semantic evaluator/runtime correctness.

**Preservation evidence:** `~/.local/state/codewiki/evidence/wiki-doc-alignment-fw_ls3xg/` contains the initial state, worktree/index patches and the four pre-edit files. Branch/HEAD are `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`; the index is empty and all 196 non-plan paths matched the preceding capture before starting. No delegation is needed for this four-file documentation packet; the parent owns the single writer and review.

**Status: complete, documentation only; uncommitted.** Parent review inspected the scoped diff and distinguished accepted concepts from unresolved representation choices. Primary/auxiliary LSP diagnostics checked all four files with zero findings, unsupported files or inconclusive results. The bounded link check resolved all 15 relative prose links. Native checks confirmed byte-identical frontmatter in all three Wiki documents and an unchanged Project repository-identity/binding safeguard. Scoped whitespace checks passed. These are document/preservation checks, not semantic evaluator or runtime qualification; no build or source suite was rerun. `document-checks.json`, the final scoped patch and the final preservation comparison are retained in the evidence directory above.

#### W1-PROFILE: active design packet

**Execution authority and division of work.** The maintainer approved proceeding with this next packet and authorized dev delegation under parent PM scope/validation. Start with a read-only compatibility audit by one `dev` in the existing checkout; the parent remains the only repository writer, limited initially to this plan. The child must not change files, stage, commit, run builds, migrate, invoke CodeWiki tools, load this mutable package, launch nested agents or perform model experiments. It returns cited current source/test contracts, a compatibility matrix and candidate future file/test scope. The parent verifies the key source claims and develops the decision table and desk examples before representing new schema choices as accepted Wiki truth. This authorization does not retroactively accept W1's illustrative fields or release the SC-2B-R hold.

**Preserved subject.** `audit/clean-baseline` at `fbc793a5713c7559f40d0c64bf953abe31bb8ce1`, empty index, existing dirty work retained. All 197 paths matched the preceding completed capture. New pre-edit evidence is `~/.local/state/codewiki/evidence/wiki-profile-design-v5vlimyr/` (`state-before.json`, `plan-before.md`, `worktree-before.patch`). Preserve all 196 non-plan paths, including the three previously aligned Wiki bodies and their envelopes, until an accepted schema decision calls for a separately reviewed in-scope prose update. No worktree, scaffold, runtime source change, lifecycle rewrite, push or activation is part of this pass.

**Validation and handoff.** Validate child citations against actual bodies, separate historical tests from fresh execution, inspect contradictions and desk-case coverage, and run plan diagnostics/whitespace plus a final hash/mode/ref/index comparison. The delegated audit is not a runtime qualification or independent semantic experiment. Subagent infrastructure/tooling failure blocks that lane: retain exact failure and partial-state evidence; do not switch execution mode silently. A native completion notification is the dependency barrier for parent synthesis; the final decision record must identify actual returned artifact paths and unresolved choices.

**Completed delegation:** native async workflow `0447cb9c-2633-492b-a7c3-c2938aa0709b`, child key `w1-profile-compatibility`, child run `abad9bab-b3ff-437a-b4d3-d36f8f778a0a`, project agent `dev`, fresh context, shared checkout with no child write authority. The returned artifact is `/home/canina7/.pi/agent/sessions/--home-canina7-Projects-codewiki--/subagent-artifacts/outputs/0447cb9c-2633-492b-a7c3-c2938aa0709b/w1-profile/compatibility-audit.md`. The terminal workflow receipt and an unchanged copy of that report are retained in the evidence directory above. Parent verification confirmed all 196 non-plan paths, HEAD, branch, refs and index unchanged after the child returned. The report is useful evidence with the corrections below, not an accepted implementation design. No retry, fallback executor or source implementation ran.

**Parent decision frame used for synthesis:** distinguish (1) exact source bytes and scoped retrieval, (2) a candidate's profile/type/body conformance, and (3) authorized acceptance of that exact candidate. Reconcile the four-field proposal with honest baseline adoption and immutable historical binding; a Change path is a navigation entry point, not an authenticated receipt. Evaluate a safe, deliberately bounded YAML data profile without claiming full OKF/Obsidian round-trip compatibility. Prefer semantic content obligations over arbitrary boilerplate, and declarative custom-type requirements over executable schema plugins. Resolve the TypeDefinition bootstrap floor and candidate-wide effects of type revisions explicitly; do not interpret old Items with mutable latest definitions or permit candidate types to approve themselves. These are evaluation criteria, not a newly frozen schema.

- **Scope:** representation decisions in this plan and, only after the relevant decisions are accepted, matching prose in the same three Wiki component specifications. No new TypeDefinition files, sample corpus fixtures, source/test changes, configuration, migrations or public API surfaces in this packet.
- **Decisions to resolve:** whether W1's four-field envelope is the correct minimum; YAML/OKF compatibility and lossless source preservation versus structured output; exact semantics and resolution of origin/revision/type references; common and type-specific body requirements; core/custom TypeDefinition bootstrap and version binding; honest baseline adoption and compatibility with existing canonical-JSON envelopes. Do not silently promote W1's illustrative keys or an inheritance mechanism to a contract.
- **Required desk cases:** direct-authored already-conformant and unstructured drafts; multi-source consolidation with disagreement; missing or historical origin evidence; custom type creation/evolution without self-authorized exemptions; rename/split/merge and hot/cold lineage; a non-software field-research Project. Describe both admitted and rejected/unresolved results without claiming an implemented evaluator or running paid model experiments.
- **Deliverable:** an explicit decision table, concrete examples and a compatibility/disposition matrix that separates source admission, managed Wiki conformance and acceptance. Keep transport/read APIs, runtime-specific UI/TUI tooling and live migration as separate future scopes.
- **Exit:** owner acceptance of the proposed representation choices and explicit approval of a revalidated bounded implementation packet. W1-PROFILE design execution was authorized; P1's new choices are not automatically adopted. The source-versus-Wiki distinction now explains why raw-source repairs do not technically depend on YAML/type syntax, but SC-2B-R still requires its own approval.

#### W1-PROFILE P1: parent-reviewed recommendation

**Status: proposed design, awaiting owner acceptance.** P1 recommends the following contract rather than leaving several competing profiles implicit. It does not modify the desired-state Wiki to pretend that these new choices were already agreed, and does not freeze a parser/library, numeric execution budgets, acceptance-receipt wire protocol or public API. Those execution choices must be explicit in their own bounded packets.

##### Audit disposition and corrected executable baseline

Parent verification read the actual bodies of `decodeWikiFile`, `decodeMarkdownItem`, `decodeYamlItem`, `decodeWikiItemEnvelope`, `semanticProjection`, `partitionWikiAttributes`, `validateWikiTree`, `decodeWikiInlineLinks`, `prepareWikiPatch`, `commitDecision`, `readExactWiki`/`readWikiTree` and `loadMarkdownMaterialSource`. A standalone filesystem/JSON-header census found **47 Markdown files under `.codewiki/wiki/items/`**, not three; the three previously edited files were only the W1-DOC edit scope. This census did not run a CodeWiki decoder or establish acceptance.

| Child finding | Parent disposition |
| --- | --- |
| W1 YAML is incompatible with the current decoder and Item contract | **Confirmed.** `src/kernel/wiki/file.ts` requires one canonical-JSON frontmatter line; its `.yaml` reader is also a canonical-JSON profile. `src/kernel/wiki/item.ts` requires the old exact key set, protocol and namespaced identities. W1 does not work by merely renaming `itemType` to `type`. |
| Acceptance is not implemented anywhere | **Rejected as overbroad.** `src/server/commands/lifecycle.ts:commitDecision` already verifies a Decision Gate, writes Change events and uses expected-old-OID canonical/Change ref updates; `prepareWikiPatch` decodes upserts and validates a joined Wiki transaction. These are existing executable mechanisms, not proof of the successor SC-4 semantic/adoption protocol. Raw material is not automatically adopted. |
| Historical/Change links have no legal home | **Rejected as overbroad.** Ordinary Markdown body links are representable. `src/kernel/wiki/links.ts` validates the special `codewiki://item/` syntax, not every Markdown link; tree liveness checks apply to those targets and explicit Item relationships. Namespaced attributes can carry inert canonical data, including paths/citations. What is absent is the proposed fields' standardized resolution and acceptance meaning—not the ability to represent a path. |
| Type is semantically weak today | **Narrowed.** `semanticProjection` excludes legacy-prefixed type/attribute/provenance meanings; ordinary namespaced `itemType` remains in the digest. Do not generalize the deliberately isolated legacy behavior to all types. |
| New type-home files are writable but invisible to all reads | **Narrowed.** The exact managed Wiki reader enumerates `.codewiki/wiki/items`, while lifecycle commit scope uses the wider Wiki prefix. Normal patch upserts still pass `decodeWikiFile`, which rejects paths outside the old Item root. Raw Git/material reads can observe other paths. The differing scopes need coherent treatment, but the audit did not prove an admitted write bypass or universal read invisibility. |
| Corpus/material layer is implemented but unaccepted | **Split.** SC-2A's pure corpus has its recorded completed bounded scope; SC-2B integration is locally implemented and unaccepted. Do not transfer the latter's defects or status to the former. No tests were rerun for this audit. |
| Relax the existing exact-record decoder and make homes configurable | **Not accepted.** Neither follows from W1. Preserve existing decoders/fixtures and fixed structural homes; assess a separately versioned profile deliberately. The proposed test paths must use actual `.test.mjs` files. No architecture allowlist change is automatic. |
| SC-2B-R does not parse frontmatter | **Confirmed for the helper's body.** It resolves a source and loads bytes before invoking the passive corpus. Format/type decisions do not themselves fix or block its limit, empty-file, typed-cause and native-read repairs. Native Store defect claims remain the earlier recorded probe evidence, not newly reproduced results. |

##### Recommended representation decisions

| ID | P1 recommendation | Boundary / reason |
| --- | --- | --- |
| P1-1 Source versus output | Preserve exact input bytes. Governed output is Markdown with a bounded YAML data mapping in frontmatter, UTF-8/LF and no BOM; any required conversion is an explicit proposed transformation. Do not normalize arbitrary prose Unicode during reads. | Source eligibility is broader than managed-profile conformance. Tolerating source syntax is not silently rewriting it or adopting it. |
| P1-2 YAML safety | Use JSON-compatible data values, string keys and unique mapping keys. Reject custom tags, aliases/anchors, merge keys, extra YAML documents and unsupported implicit scalar forms for managed conformance; never evaluate code. String fields must actually be strings. | This is a deliberately restricted YAML profile, not full YAML/OKF/Obsidian round-trip compatibility. Unsupported source bytes remain readable material under the applicable source limits. Parser and finite byte/depth/node/list budgets must be fixed before implementation. |
| P1-3 Common fields | Retain W1's required `type`, `title`, nonempty unique `codewiki-origin` list and `codewiki-revision` string. `description` is an optional string; `aliases` and `tags` are optional unique string lists. | No compulsory UUID, status, timestamp, summary or complete history list. The four fields provide meaning and navigation, not proof of acceptance. Document-level origins do not attribute every sentence. |
| P1-4 Extensions | Preserve bounded unknown data fields without granting them authority. Reserve `codewiki-*` for declared profile meanings; unknown reserved fields fail managed conformance instead of silently doing nothing. `sources` may be preserved as source/citation data, but P1 does not freeze an OKF source-entry schema. | An imported `approved`, `active` or `verified` field is still data. Necessary grounds can use body citations and exact Change bindings without a mandatory metadata schema for every paragraph. |
| P1-5 Change references | Origin/revision values are Item-relative paths into the Project's `.codewiki/changes/` namespace. Resolve lexically within the selected Project, without filesystem traversal, symlink following or permission expansion. Retain creating/adopting origins across ordinary revision; the revision pointer names the Change responsible for the current accepted Item bytes. | Navigation paths are not immutable receipts. Exact prior/resulting subjects and historical Change versions belong in retained Change/Git bindings; no self-enclosing commit hash is demanded in frontmatter. A missing historical target is reported unresolved, not guessed from a same-named current path. |
| P1-6 Identity and continuity | An exact Item is identified by its selected snapshot, path and retained bytes/blob identity. Rename, split, merge and retirement Changes explicitly map old and resulting subjects. Do not infer continuity solely from title, similar text or Git rename heuristics. | Update affected relative links/pointers in the assessed candidate. Preserve old IDs/provenance in retained old subjects and adoption mappings; do not erase them in a live conversion or invent absent origins. |
| P1-7 Type resolution | Keep the six reserved core names. Recommend case-sensitive ASCII PascalCase names for additional types; resolve each custom name to exactly one `TypeDefinition` whose `title` declares that name under `types/`. Aliases are navigation only. | Duplicate/unknown declarations are unresolved or invalid for managed use, not arbitrary first-match resolution. Display prose can use normal language. The catalogue is derived from bound documents, not a second authoritative registry. |
| P1-8 Custom forms | A custom TypeDefinition declares `codewiki-base` naming exactly one of Definition, Entity, Contract, Procedure or Claim, and adds explicit content obligations in prose. No deep/multiple inheritance and no executable validation DSL in the first profile. | This additional field is required only on custom TypeDefinitions. It cannot remove the core floor, acceptance requirements or the meaning of the base form. Custom meta-types refining TypeDefinition are deferred. |
| P1-9 Body obligations | Require a title-matching first H1 and concise primary meaning. Require the type's semantic content below, but not universal boilerplate or exact H2 labels. Extra sections are allowed when useful. | H1/field checks are structural. Adequacy, applicability, exceptions and uncertainty require assessment; a heading checklist is not that assessment. Explicitly state unavailable grounds where material rather than fabricate them. |
| P1-10 Bootstrap and evolution | An authorized initial Change proposes the six core TypeDefinition documents under a selected immutable supported profile, including TypeDefinition itself. The profile supplies the non-waivable bootstrap floor; candidate text does not grant its own authority. Custom types and their first instances may be one assessed candidate under that floor. | Type revisions require impact assessment of affected Items, including byte-unchanged Items whose interpretation would change. An unchanged Item cannot silently acquire unassessed new obligations. A stricter candidate type adds checks; it cannot waive the governing baseline. |
| P1-11 Interpretation version | Bind interpretation to the supported profile/version and exact type-definition snapshot used for the recorded subject. Keep this context in source/Change bindings rather than repeating a fifth universal field in every Item. | A stand-alone exported file without this context remains material with unresolved acceptance/version. Historical reads never substitute mutable latest definitions. Exact receipt/profile-selection encoding is an SC-4/integration prerequisite, not selected here. |
| P1-12 Existing compatibility | Keep current canonical-JSON envelopes, IDs, namespaced metadata and existing readers/tests unchanged. Any future successor interpreter is an explicitly selected version, not fallback after a failed old decode. | No live rewrite, mixed-profile auto-adoption or blanket decoder relaxation. Historical representation/assurance remains observable. A future conversion needs exact old/new bindings and separate authority. |

##### Minimum semantic body content

| Type | Required meaning, not mandatory H2 spelling |
| --- | --- |
| Definition | Meaning, relevant scope and distinctions from easily confused concepts. |
| Entity | What the thing is, its role/boundary and consequential relationships; intended versus observed state where they differ. |
| Contract | Applicable scope, obligations and material exceptions or limits. |
| Procedure | Preconditions, ordered actions, expected outcome and consequential failure/recovery conditions. |
| Claim | Assertion and scope, grounds or their declared absence, uncertainty and material counterevidence. |
| TypeDefinition | Purpose, use/avoid conditions, core relationship, required content and assessment limits. Core declarations cannot redefine the bootstrap floor. |

##### Illustrative non-software candidate

These examples are invented desk material, not existing files, Changes, observations or accepted outcomes. The cited initial Change would propose both the custom type and its first Item; baseline authority/profile checks still govern their assessment.

At `.codewiki/wiki/types/field-observation.md`:

```yaml
---
type: TypeDefinition
title: FieldObservation
codewiki-origin:
  - ../../changes/establish-survey-knowledge/
codewiki-revision: ../../changes/establish-survey-knowledge/
codewiki-base: Claim
---
```

Example body: `# FieldObservation`, followed by a definition of a scoped survey finding. Its required content is the observation, site/time/method, exact supporting log and sampling limitations; use it when independent assessment is useful, not for every raw data row. It inherits Claim's grounds/uncertainty obligations. This is declarative prose, not an executable schema.

At `.codewiki/wiki/fieldwork/marsh-survey.md`:

```markdown
---
type: FieldObservation
title: Marsh survey finding
codewiki-origin:
  - ../../changes/establish-survey-knowledge/
codewiki-revision: ../../changes/establish-survey-knowledge/
---
# Marsh survey finding

The example survey recorded six detections; this does not establish six distinct animals.

## Conditions and grounds

The Change must bind the exact log, study site, observation interval and survey method.
Those details are not supplied in this illustration, so adoption remains unresolved.

## Uncertainty

Repeat detections and limited visibility prevent an abundance estimate.
```

A structurally plausible document is not automatically an adequately grounded FieldObservation. In a completed candidate, actual site/time/method and retained log bindings replace the unresolved placeholders without manufacturing a stronger conclusion.

##### Compatibility and desk-case matrix

All outcomes below are design reasoning, not executed profile tests. “Conformant” is conditional on the selected profile, resolved types and required content; “accept” additionally requires an authorized assessment of exact consequences.

| Input or transition | Source admission / proposed conformance | Acceptance disposition |
| --- | --- | --- |
| Already adequate direct-authored Item | Inspect exact bytes; preserve content that already meets the chosen profile. Current legacy decoder still does not read P1 YAML. | Assess the exact candidate; no compulsory paraphrase and no acceptance merely because a human wrote it. |
| Plain README, CRLF/BOM notes or unknown imported type | Can be source material under the applicable limits; not thereby P1-conformant managed knowledge. | Change may propose a transformed/consolidated Item; retain original bytes and meaning-bearing differences. |
| Existing canonical-JSON Item | Continue its existing representation and recorded assurance. It is not a P1 Item by key renaming or YAML parsing. | No automatic re-adoption, stripping of IDs, invented historical origins or live conversion. |
| Several sources disagree | Cite exact versions and preserve the conflicting claims/conditions; a concise combined assertion may be inadequate. | Accept a properly qualified conclusion only if its required grounds/assessment support it; otherwise unresolved. |
| Earlier creator is unknown | An initial adoption Change records the observed baseline now; origin points to that adoption, not an invented ancient Change. | Stated assurance is limited to observed/adopted facts; missing earlier history is explicit. |
| Origin path is absent, private or reused | Bytes/field shape may be inspectable; resolve exact historical grounds through retained bindings and permissions, not latest-name substitution. | Required unavailable evidence leaves acceptance unresolved. Reading retained old evidence does not re-adopt it. |
| Custom type and first Item arrive together | Assess the definition and instance under the established bootstrap/base floor plus proposed added requirements. | Unknown authority, duplicate names, circular bases or candidate-authored exemptions block the relevant adoption. |
| Type is revised; instance bytes do not change | Compare interpretation and required content against the old and candidate definitions. Identify affected instances. | Do not transfer an old assessment to newly imposed semantics silently; resolve the impact before adoption. |
| Rename / split / merge | Exact old/new subjects and link updates are in the proposal; keep origin lineage and record what was retained, divided or consolidated. | Accept explicit continuity/effects, not an inferred title/path similarity. Frontmatter does not claim sentence-level attribution. |
| Hot Item is retired | Proposed deletion/removal from active guidance retains reachable exact history and creating/revising/retiring relationships. | Acceptance governs retirement; cold retrieval does not revive authority, and stale support alone is not retirement. |
| Marsh-survey example above | Plausible field shape, but actual conditions/log are unavailable and required content is incomplete. | **Unresolved**, not a fabricated empirical success or a newly accepted observation. |

##### Next bounded coding recommendation and gates

**Recommend revalidating SC-2B-R first, not rewriting the Wiki decoder.** The now-agreed source-versus-managed-Wiki distinction is compatible with its passive exact-byte input purpose. P1's YAML/type choices are not a technical prerequisite for these raw-source repairs. Its old optional-profile wording applies to source material only, not to adopted Wiki conformance. Keep the existing hold until explicit repair approval; do not use this clarification as a retrospective authorization.

The proposed coding scope remains `src/server/queries/material-source.ts`, `src/server/queries/source.ts` only for lossless internal resolution with the old wrapper preserved, read admission/order in `src/adapters/git/project-store.ts`, `tests/server/queries/source-material.test.mjs`, `tests/adapters/git/project-store.test.mjs`, targeted closure assertions in `tests/kernel/invariants/architecture.test.mjs`, and this plan. Preserve SC-2A corpus files, `local-server.ts`, frozen read-purity tests, all Wiki/lifecycle files, settings, outputs and every unrelated path. The earlier SC-2B-R regression matrix remains required; historical green tests are not acceptance evidence. Recheck Store source, exact worktree subject and test/build commands when authorizing that implementation.

For a later P1 implementation, require an explicit internal versioned profile-reader packet before public integration: finite parser budgets and dependency selection, positive/negative metadata/body fixtures, current-envelope regression isolation, deterministic type resolution with no hidden registry, historical binding cases and non-software cases. Do not bundle new roots, a migration, lineage writers and lifecycle replacement into a relaxed old decoder. Public reads and successor acceptance writes remain separately bounded. Parent PM reviews actual dev diffs and command outcomes; a future implementation also receives fresh-context read-only review before acceptance.

**Review exit.** Parent source verification and desk synthesis are complete; P1 remains a recommendation awaiting owner acceptance. No additional Wiki edits, source tests/builds, native Git mutation, migration, installation, model experiment or fallback executor ran. Retained evidence includes the original dev report, terminal workflow receipt, `audit-return-verification.json`, and the pre-synthesis plan. Validation passed: the explicit plan LSP check returned zero diagnostics with no unavailable/inconclusive result; session diagnostics reported no issues; scoped `git diff --check` passed; all 196 non-plan captured paths and HEAD/branch/refs/index were unchanged, with no new non-ignored paths. The final comparison and scoped patch are retained as `preservation-after.json` and `final-plan.patch`. These documentation gates do not qualify a parser, semantic evaluator or repaired SC-2B.

## Historical evidence: cold Git path

The full prior ledger, brainstorm rationale, research sources, predecessor audits, failure records and qualification lineage remain byte-for-byte at commit `e8587493759a564e2b92ceb0e1912cbf74c1ce54`, path `SEMANTIC_KERNEL_PLAN.md`, blob `63e33716f3b692f139bb1a52295838a6103fd710`. No duplicate archive file is needed. Retrieve only when the current question requires its evidence:

```bash
git show e8587493759a564e2b92ceb0e1912cbf74c1ce54:SEMANTIC_KERNEL_PLAN.md
```

Old “current”, “next”, queue and authority wording describes those historical subjects, not present instructions. Existing `docs/archive/**` and governed lifecycle records remain unchanged. This native documentation checkpoint neither rewrites their outcomes nor claims a CodeWiki-governed admission, retirement, release, activation or controller handoff.
