/**
 * One release version owns the ChangeKernel contract, including managed Wiki
 * representation and interpretation. The current distribution shares this version
 * with the Product package. A version label is not an exact build or qualification.
 */
export const CHANGEKERNEL_VERSION = "0.4.0-sk3g.1" as const;
