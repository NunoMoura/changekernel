export const BACKEND_OBSERVABILITY_PROTOCOL = Object.freeze({
	id: "codewiki.backend-observability",
	version: "1.0.0",
} as const);

export const BACKEND_FAULT_RECOVERY_PROTOCOL = Object.freeze({
	id: "codewiki.backend-fault-recovery",
	version: "1.0.0",
} as const);
