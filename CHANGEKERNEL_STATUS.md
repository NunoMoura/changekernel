# ChangeKernel source-development status

This is the short pre-dogfood gap note, not a second design specification or roadmap. Wiki owns desired state; qualified Change/Work will replace this note after explicit adoption and controller handoff. Read `AGENTS.md` before coding. Never load this mutable checkout as its own controller or invoke ChangeKernel lifecycle tools here.

## Current checkpoint and verified progress

- Work started on the verified `audit/clean-baseline` branch at reviewed checkpoint `d005f604ef2730eed106855fc9f5d94e0b6d550d`, with only `outputs/` untracked and both existing stashes present. This slice is source development, not a qualified operational release. Git records its resulting checkpoint. The branch, stashes and `outputs/` are preserved.
- Step 1 is implemented internally in `src/kernel/gates/checks.ts`: explicitly versioned unified Check, Pack, adoption, input and result contracts; strict bounded decoding and content identities; exact Wiki/Change provenance, Pack/definition pins, parameters, permissions, execution budgets, model settings and subject bindings. Computation and bounded inference share one contract. No first-party policy is selected implicitly.
- Deterministic selection binds the current host-supplied baseline, candidate, stage and actual effect facts. Candidate policy cannot replace the current adoption binding. Unknown applicability remains unready; missing required input cannot deactivate a Check. Completed results have Boolean outcomes, actionable feedback, Evidence references and limitations. Operational errors have no fabricated semantic Boolean. Exact-result reduction rejects missing, foreign or duplicate results and cannot call an empty selection validated.
- Superseded experimental Gate/findings, the structural-transition Check wrapper and the internal intent-fit judge were removed with their obsolete tests. Structural validation and authorized source/output verification remain. The historical `codewiki.decision-check-output@1.0.0` decoder moved to `decision-output.ts` without changing its format or schema identity; a regression pins that identity. Historical records and identifiers were not rewritten, and no compatibility aliases were introduced.
- Public operations remain Project discovery/capabilities, `changes.propose-profile`, and individual Changes get. The new contracts are not connected to lifecycle commands or execution. Wiki remains the durable design authority: [Checks](.changekernel/wiki/items/system/components/checks.md), [Evidence](.changekernel/wiki/items/system/components/evidence.md), and the [minimum release](.changekernel/wiki/items/system/components/package.md#minimum-local-dogfood).

## Remaining limits and next bounded task

These contracts verify consistency, not authority or truth from self-supplied hashes. The backend must authenticate current adoption and grants, resolve actual effects and source bytes, and establish Evidence custody and freshness. Input declarations currently support bounded canonical value shapes; activation supports declared stages and effect kinds, with incomplete effect information retained explicitly. No isolated execution, containment, model primitive, adopted first-party Pack, durable Evidence/result storage, or lifecycle progression is implemented by this slice.

The next bounded task is step 2: prove one isolated TypeScript/JavaScript runner with bounded inputs, read-only execution and one authorized structured model-call primitive, including a supported local/private route and honest containment limits. Add only a small explicitly adopted first-party Pack for the supported loop. Do not perform live or paid execution without separate applicable authorization.

After that, remaining gaps are checkpointed sources and durable Evidence/results, exact Decision acceptance and reports, fresh-session Planning and bounded Work/Review, then qualification of immutable external candidate bytes. Activation and explicit controller handoff remain separate. Python, marketplaces, Hub, broad connectors, automatic routing, parallel workers and advanced caching are not first-release work.

## Verification and preserved evidence

- Focused contract, historical-format and architecture/determinism tests: **45/45 passed**. One obsolete architecture assertion initially referenced the removed intent-fit module; it was replaced with a boundary assertion for the unified contracts.
- Final `npm test`: **465/465 passed**, exit status 0, including type checks and build. The preceding checkpoint had 479 tests; removal of superseded suites and addition of focused regressions explains the changed count.
- Active language-server probes covered all 14 changed executable files still present, with no errors or unavailable/inconclusive results. Existing hints and auxiliary style findings remain in retained code; the new contract boundary has advisory hints about deliberately decoded unknown inputs and runtime shape checks.
- `git diff --check` passed. Test logs are `/tmp/changekernel-step1-focused.log` and `/tmp/changekernel-step1-test.log`; they are development evidence, not release qualification.
- Earlier checkpoint evidence remains at `/tmp/changekernel-wiki-foundation-8jlm55pp/`, `/tmp/changekernel-naming-_uo2k1zx/`, `/tmp/changekernel-rename-cleanup-jpgs89fl/`, and `~/.local/state/codewiki/evidence/decision-intent-fit-wfT3h5/`. No historical Change traces, Wiki identities, procedure files, stashes or untracked outputs were modified. Source commits and pushes require explicit user authorization. No lifecycle operation, live model call, installation, qualification, activation or controller handoff was performed.
