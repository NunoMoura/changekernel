# ChangeKernel source-development status

This is the short pre-dogfood gap note, not a second design specification or roadmap. Wiki owns desired state; qualified Change/Work will replace this note after explicit adoption and controller handoff. Read `AGENTS.md` before coding. Never load this mutable checkout as its own controller or invoke ChangeKernel lifecycle tools here.

## Current checkpoint and verified progress

- Branch: `audit/clean-baseline`. Reviewed starting checkpoint: `d005f604ef2730eed106855fc9f5d94e0b6d550d`. Step 1 was committed with user authorization as `0e9d4c27a`; its push failed with GitHub SSH `Permission denied (publickey)`. No remote update or credential change was made. The runner and DSH bridge form one user-authorized source checkpoint, recorded together in Git. Both existing stashes and `outputs/` are preserved. None of this is a qualified operational release.
- Step 1 provides explicitly versioned unified Check, Pack, adoption, input and result contracts in `src/kernel/gates/checks.ts`. Exact current Wiki/Change, subject, effect, implementation, permission and budget bindings remain mandatory. Missing input stays unready; operational inability cannot fabricate a semantic Boolean. No first-party policy is selected implicitly. Superseded internal Gate/findings and intent-fit execution were removed; historical interpretation bytes and schema identity remain unchanged.
- Step 2 is **partially implemented**: a private Linux runner proof in `src/adapters/checks/`, a shared bounded model port in `src/ports/check-model.ts`, and its DSH implementation in `src/adapters/dsh/check-model.ts`. JavaScript executes as an exact supplied single-file artifact; TypeScript must be compiled ahead of execution. Runtime/library and worker identities are bound to the adopted definition. The versioned worker interface exposes bounded frozen inputs and one structured model primitive. Custom modules are never loaded into the Project Server process.
- The host uses Bubblewrap and user-service controls with copied runtime bytes and disposable owner-private storage outside the checkout. It verifies effective kernel memory, swap and task limits before allowing the worker to import custom code. The sandbox has no host project, home, credentials or network access; Node permissions add restrictions. Time, output and input limits, process-group cancellation, service closure, exact model route/settings and structured responses are checked. A host permits one execution at a time, tracks at most 256 exact attempted inputs and blocks after unresolved provider/process custody. There is no in-process fallback.
- Check inference now uses the actual DSH model registry, prepared-call interface and stream assembler, with exact provider/model/configuration and sampling identities. Each auxiliary call has a fresh model registry without producer history, agent loop, tools, automatic retry or fallback. Explicit completion, bounded canonical structured output and token usage are required. Unknown closure poisons the model bridge and blocks the Linux host. DSH remains the owner of harness mechanics and provider transport; no parallel provider client was added.
- Positive computation and Check-to-DSH tests run in disposable external directories with scripted DSH providers, not live inference. Hostile-write, reachable host-network listener, subprocess, memory, timeout, cancellation, malformed output, provider-budget and cleanup-uncertainty cases are covered. Public operations and lifecycle entrypoints remain unchanged. Wiki owns durable design: [Checks](.changekernel/wiki/items/system/components/checks.md), [Evidence](.changekernel/wiki/items/system/components/evidence.md), and the [minimum release](.changekernel/wiki/items/system/components/package.md#minimum-local-dogfood).

## Remaining limits and next bounded task

The runner proof requires Linux x64 with the expected glibc libraries, Bubblewrap, a working user service manager and effective memory/swap/task controls. Integration tests exercise this host profile; they do not establish portable support or release qualification. Runtime preparation and the backend remain trusted. Check code receives no direct provider access, but the injected provider adapter remains trusted to enforce its configured locality, tool prohibition, usage accounting and cancellation. An input-token reserve bounds calls conservatively; it is not a qualified tokenizer or cost model.

The backend must still authenticate real current adoption and grants, resolve actual source/effect bytes and establish Evidence custody and freshness. Attempt tracking and temporary files are not durable storage or replay protection across host restarts. The DSH bridge has not yet been wired to a pinned concrete local/private DSH provider implementation; scripted providers do not establish a supported deployment route. The current provider-neutral DSH request interface also lacks provider-constrained structured decoding: canonical JSON is requested and strictly validated, with no repair or resampling. There is no first-party Pack, retained model trace, or lifecycle wiring yet.

The next bounded task is to finish step 2 by selecting and pinning one DSH provider implementation that supports an explicitly configured local/private endpoint, then wiring its backend installer with exact settings, bounded transport and usage/cancellation behavior. Test that DSH provider against a local simulated server without live or paid inference. Do not write a parallel ChangeKernel provider client or agent harness. Then add a small first-party Pack through the same Check interface and explicit adoption, with reviewed cases. Keep durable Evidence/results, Decision acceptance, Planning, Work/Review and release qualification out of that slice. No live or paid execution is authorized by this note.

After step 2, remaining gaps are checkpointed sources and durable Evidence/results, exact Decision acceptance and reports, fresh-session Planning and bounded Work/Review, then qualification of immutable external candidate bytes. Activation and explicit controller handoff remain separate. Python, marketplaces, Hub, broad connectors, automatic routing, parallel workers and advanced caching are not first-release work.

## Verification and preserved evidence

- Focused DSH bridge, isolated runner and architecture tests: **65/65 passed**. These include real DSH dispatch with scripted providers and an isolated Check reaching that path. Early runner tests exposed a missing explicit Bubblewrap user-namespace flag; it was corrected before claiming containment.
- Final `npm test`: **513/513 passed**, exit status 0, including type checks and build. The prior runner proof passed 488 tests; step 1 passed 465; the original reviewed checkpoint passed 479 before obsolete suites were replaced.
- Active language-server probes covered all seven uncommitted executable files. The final all-server probe had no errors or unavailable/inconclusive results. Advisory findings remain, including the dynamic active-promise-set style warning and existing deprecated compiler-property hint.
- `git diff --check` passed; new source/test files were also checked separately. Current logs: `/tmp/changekernel-dsh-check-focused.log` and `/tmp/changekernel-dsh-check-test.log`. Earlier runner logs: `/tmp/changekernel-runner-focused.log` and `/tmp/changekernel-runner-test.log`. These are development evidence, not release qualification.
- Step 1 logs remain at `/tmp/changekernel-step1-focused.log` and `/tmp/changekernel-step1-test.log`. Earlier evidence remains at `/tmp/changekernel-wiki-foundation-8jlm55pp/`, `/tmp/changekernel-naming-_uo2k1zx/`, `/tmp/changekernel-rename-cleanup-jpgs89fl/`, and `~/.local/state/codewiki/evidence/decision-intent-fit-wfT3h5/`. No historical Change traces, Wiki identities, procedure files, stashes or untracked outputs were modified. No lifecycle operation, live model call, installation, qualification, activation or controller handoff was performed. Source commits and pushes require explicit user authorization.
