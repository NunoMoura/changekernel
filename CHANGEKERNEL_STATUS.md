# ChangeKernel source-development status

This temporary note records unfinished pre-handoff source work. Desired behavior belongs in the [native Wiki](.changekernel/wiki/); Changes and Work take over execution state after a qualified immutable external controller is available and explicitly handed control. Git holds checkpoint history.

## Current checkpoint

- Branch: `audit/clean-baseline`; this documentation checkpoint follows pushed stage-terminology checkpoint `e36441af3`. Pi **0.85.1** remains pinned. The [transversal/backend and domain/Check boundary](.changekernel/wiki/items/system/components/changekernel.md#backend-validation-across-four-stages) is unchanged.
- Current documentation distinguishes [stages, gates and feedback loops](.changekernel/wiki/items/system/components/changekernel.md#stages-gates-and-feedback-loops). Regression coverage verifies shared Check selection across all four stages and rejects cross-stage verdict substitution; this does not implement the remaining backend stage contracts.
- Private [Decision validation](.changekernel/wiki/items/system/components/decision.md#bounded-assessment-material-and-execution) now has ten release-owned conditions, explicit source roles, separate backend input/result identities and combined result admission. All ten and the required adopted domain assessment must pass. Missing material, other stages, stale bindings, caller replacements and domain-result relabeling are rejected. Admission is not approval or a lifecycle mutation.
- Backend conditions use the existing isolated worker, bounded model route and [durable Check execution log](.changekernel/wiki/items/system/components/agent-execution.md#durable-check-attempts-and-completed-results), without fabricated Pack adoption. The trusted host must supply its verified Kernel Build; input and prompt budgets are checked before reservation. True and false results survive a fresh process without inference, under current delivery authority. The internal Linux runtime dependency identity is now `@3.0.0`; this is not a second public lifecycle version.
- Retained domain results can now be read without launching work: `readRetainedCheck` checks current authority and exact bindings, returns missing results as unavailable and preserves false results and stopped failures. The component is named **Check execution log**; stored paths and protocol identifiers are unchanged. Authenticating the governing Check policy remains a separate requirement.
- Private [stored source assembly](.changekernel/wiki/items/system/components/decision.md#stored-source-assembly) now reconstructs the exact proposed Wiki Change through the existing Project Store reader, preserving full bodies and exclusions. `readDecisionSourceMaterial` snapshots the request/configuration and rechecks current access and observed Project/Change heads before delivery. Missing or inconsistent material, revoked access and observed drift fail closed. This is not a reference lock or a ready assessment: accepted Check policy, retained domain-result joining and semantic coverage remain separate.
- This is private source/execution/admission infrastructure, not a complete Decision service. The trusted backend still owes current Check policy, domain-result provenance and authority at execution and acceptance. The public lifecycle remains unconnected to this path. Existing experimental Decision helpers are not a substitute for the new contract.

## Paused for brainstorming and remaining gaps

Implementation is paused at the user's request to discuss newly released technology before choosing further work. The agreed [Check terminology and activation](.changekernel/wiki/items/system/components/checks.md#check-terminology-and-activation) and [Check execution log/Change trace separation](.changekernel/wiki/items/system/components/agent-execution.md#check-execution-log-and-change-trace) are documented; this checkpoint changes no runtime or persisted format.

The previously planned next slice, subject to that discussion, is the accepted Wiki Check policy declaration reader: the existing Check adoption record decoder validates supplied records but does not authenticate their adoption. Resolve explicitly adopted Checks from the governing Check policy, derive activation from stage, Change type, scope and actual effects, join their exact retained evaluations to the stored source material, and account for required grounds and commitments before preparing backend Decision assessment. Do not infer semantic completeness from the file inventory or enable public admission prematurely. Develop the software Pack's meaningful, independently reviewed positive/negative and adversarial cases, qualify common judgment quality, and implement the remaining stage contracts. Connect one complete lifecycle only with explicit domain adoption, retained Evidence and separate approval.

Public lifecycle integration, semantic quality, Pack adoption, producer containment/recovery and exact external qualification remain unfinished. Custody is limited to 256 attempts, with no automatic archival, reset or retry. Physical crash/disk durability, privileged history rollback and operator-authorized reconciliation remain outside demonstrated recovery. Actual model deployment, routing and credentials still require qualification; unchanged stored bytes do not establish external freshness. No live/paid inference, activation or controller handoff is authorized by this source-development work.

## Verification

- This checkpoint is documentation-only: 27 local links checked and metadata in three Wiki files preserved unchanged. Prior executable-test results below are retained as earlier evidence, not new test runs. No implementation or technology evaluation was started during this pause.
- **625/625 repository tests passed**, including type checks and build; log `/tmp/changekernel-stages-gates-loops-full.log`. The shared Check suite also passed **19/19** against the previously installed external package, including the new four-stage regression; log `/tmp/changekernel-stages-gates-loops-installed.log`. Current compiled code matches those installed bytes. The earlier **176/176** installed-candidate smoke, **136** matching distribution files and eight pinned Pi **0.85.1** instances remain recorded at `/tmp/changekernel-decision-source-proof-0ymlkP/`; no new package or release qualification is claimed.
- These tests cover contracts, execution, restart reuse, ownership rejection and failure handling. They do not qualify semantic judgments, private routing, physical crash durability or controller authority. All 20 changed paths passed scoped language-server checks. Documentation checks verified 108 local links, unchanged metadata in 17 Wiki files and valid lifecycle diagram paths. Type checks and executable tests passed.
- Generated state, package artifacts and runtime scratch remain outside project Git. Existing untracked `outputs/`, stashes and historical Change records are preserved.
